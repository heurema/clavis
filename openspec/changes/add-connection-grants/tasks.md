Four slices in dependency order; implementation and independent acceptance review are separate tasks, and a reviewer never approves their own implementation. Slice 1 lands first; slices 2 and 3 depend on it and can run in parallel against fakes; slice 4 needs everything. Expect roughly 700 to 900 changed handwritten lines; generated sqlc/templ output reported separately.

## 1. Contracts, migration and queries

Requirements: connection-grants "Grant records" (shape), "Per-request connection authorization" (codes); user-administration "Administrator-only user operations" (UUID-shaped usernames refused, username addressing); local-authentication "Secret-free authentication events" (allowlists, connection column, migration). Owned paths: `internal/auth/grants.go`, `internal/auth/connections.go` (`ConnectionSummary`), `internal/auth/contracts.go` (`Identity` extension), `internal/auth/password.go` or `validation.go` (`ValidUsername` UUID exclusion, `ValidUserRef`), `internal/auth/errors.go`, `internal/auth/events.go`, `internal/database/migrations/004_grants.sql`, `internal/database/queries/grants.sql`, `queries/auth.sql` (`InsertAuthEvent` connection column, `FindUserByUsername`), `internal/database/sqlc/`, `internal/database/auth.go` (`audit` gains a connection argument; callers pass empty), `internal/database/initialize.go` (column checks), related `_test.go`. Verify: `make generate-db && make check-db-generated && make check-sql-boundaries && go test -race ./internal/auth/ ./internal/database/ -run 'Migrat|Generated|Event|Grant|Username|Ref'`.

- [x] 1.1 Add grant DTOs, `Grants` interface, `ConnectionSummary`, `Identity` connection fields, `ValidUserRef`, the UUID-shape exclusion in `ValidUsername`, `CONNECTION_DISABLED`, the grant actions and outcome; verify allowlist tables, `Event.Valid`, redaction, and that bootstrap and create refuse UUID-shaped usernames with a hint.
- [x] 1.2 Add migration `004` (grants table with foreign keys and index, `auth_events.connection_id`, username constraint replacement, widened allowlists) and the named queries; extend `InsertAuthEvent` and the `audit` helper; add `CheckGrantsColumns` to readiness; regenerate; verify fresh and initialized databases apply `004` once, the username constraint rejects a UUID-shaped name, grant uniqueness and foreign keys hold, granted-connection queries join correctly with selectors, and the CLI-only build stays Go-only.
- [x] 1.3 Independent acceptance review of slice 1; record the revision and generated size. Reviewed 2026-09-12 on the working tree that became `2d49969`: ACCEPT WITH FOLLOW-UPS, no blocking findings; applied before commit: a migration test proving 004 fails closed while a UUID-shaped username exists and applies once after a rename, plain aliases on the grant row identifiers, a doc comment on the member listing type, and the now-redundant UUID guard on connection names removed. Carried to slice 3: the adapter's username rejection needed the hint. Handwritten about 600 lines (contracts, migration, queries, tests); generated 357 plus small edits.

## 2. Service: grants, authorization and member reads

Requirements: connection-grants "Grant records", "Member visibility of granted connections", "Per-request connection authorization"; connection-management "Administrator-only connection operations" (member reads, delete guard count); user-administration (username addressing in every user mutation). Depends on slice 1. Owned paths: `internal/database/grants.go`, `internal/database/grants_test.go`, `internal/database/connections.go` (member read branch, delete guard count), `internal/database/admin.go` (user reference resolution, `outcomeNone`), related tests. Verify: `CLAVIS_BACKEND_TEST_DATABASE_URL=... go test -race ./internal/database/`.

- [x] 2.1 Implement `ListGrants`, `CreateGrant`, `RevokeGrant` and `AuthorizeConnection` with the design's transaction shape and a fifth advisory key; resolve users by UUID or username everywhere including the existing user mutations and session revocation; add the member branch to connection list and get returning summaries; extend the delete guard with the grant count in its hint; `administer` supports a no-event success outcome for idempotent no-ops.
- [x] 2.2 Real-PostgreSQL tests: grant by name and by UUID; idempotent create and revoke without duplicate events; unknown user or connection; member denials with events; member listing and get in the summary shape with selectors, absence of ungranted connections, `CONNECTION_NOT_FOUND` without an event, disabled shown; authorization for administrator, granted member, ungranted member, disabled connection, revoked session; revocation takes effect on the next call; grants survive block/unblock; delete guard count; rename keeps grants; dry runs leave no rows or events; injected audit-write failure; held-lock timeout; concurrent grant and connection delete serialize; sentinel usernames and names never enter events beyond UUIDs.
- [x] 2.3 Independent acceptance review of slice 2 including the authorization paths; record the revision. Reviewed 2026-09-12 on the working tree that became `e497c5e`: ACCEPT WITH FOLLOW-UPS, no blocking findings; applied before commit: the delete guard evaluates both conditions at once so a dry run on an enabled granted connection already states the count; self-target guards tested by username; the member listing denial tested by UUID as well; session validated before the selector in the member listing. Accepted decisions: `administer` bodies return a `mutation{target, connection, outcome}` with `outcomeNone` committing without an event; `LockMutationUsers` locks a username-addressed target in the same ordered statement; malformed grant references answer `USER_NOT_FOUND` with a denial event while the existing user mutations pre-validate to `INVALID_ARGUMENT`; unknown listing filters yield an empty list; `AuthorizeConnection` records its unauthenticated denial under `connection.get`. Handwritten about 1,457 added and 133 removed; generated +36/−4.

## 3. Routes and CLI

Requirements: connection-grants "Grant routes and listing"; connection-management "JSON connection routes" (member `GET`); cli-authentication "Grant commands", "Authoritative identity and explicit sign-out" (`whoami`), "Administrative user commands" (`--user` references). Depends on slice 1; tests use fakes. Owned paths: `internal/server/grants.go`, `internal/server/auth.go` (routes, rejection mapping, identity extension, `HandlerWithAuth` dependency), `internal/server/connections.go` (member shape passthrough), `internal/server/*_test.go`, `internal/cli/grants_commands.go`, `internal/cli/auth_commands.go` and `users_commands.go` (reference validation), `internal/cli/connections_commands.go` (summary rendering), `internal/cli/auth_transport.go`, `internal/cli/result.go`, `internal/cli/*_test.go`. Verify: `go test -race ./internal/server/ ./internal/cli/ && make build-cli`.

- [x] 3.1 Mount the three grant routes with the shared handler shape, member scope on listing, `whoami` extension; let the connection `GET` routes pass member sessions to the service and return whichever shape it produces; require the grants dependency in `HandlerWithAuth`.
- [x] 3.2 Add the `grants` CLI group, `ValidUserRef` on every `--user`, summary-shape validation and rendering for connections, `whoami` rendering, the per-route error allowlist; tests for every spec scenario in unit and subprocess form, plus real-database route round trips including a member's reads and denials.
- [x] 3.3 Independent acceptance review of slice 3; record the revision. Reviewed 2026-09-12 on the working tree that became `a746598`: ACCEPT WITH FOLLOW-UPS, no blocking findings; applied before commit: `CONNECTION_DISABLED` added to the CLI's per-route allowlist walk so it maps to `INVALID_RESPONSE` until a route documents it, and one shared connection-reference hint. Accepted: the CLI sends one request for an idempotent create by accepting 200 on the 201 route; revoke text renders only `Revoked:`; adapter validation on user routes relaxed to `ValidUserRef`; fixtures that used now-valid usernames as invalid values were changed. The two temporary wiring seams the worker left were replaced with the store value once slice 2 landed, and the coordinator added the real-database grant route round trip. Handwritten about 2,170 added and 113 removed (about 655 non-test).

## 4. Web, smoke, documentation and whole-change verification

Requirements: connection-grants "Read-only grants table in the browser"; all delta specs end to end; project-bootstrap quality and smoke requirements. Depends on slices 1 to 3. Owned paths: `internal/web/*`, `internal/server/auth.go` (`adminBrowser` grants call), `scripts/smoke.mjs`, `README.md`, `docs/PRD.md` (section 12 status), `reports/` (ignored). Verify: `make check && make smoke && make test-mutation`.

- [x] 4.1 Add the grants table to the admin page with rendering and page tests, loaded after the connections list and failing closed.
- [x] 4.2 Extend smoke with the grant flow from the design (grant by username, member `whoami` and reads in the summary shape, member denials, delete guard count, revoke and its effect, idempotent revoke, event counts, `users block --user <username>`).
- [x] 4.3 Update README (grants section, username references, member view, `whoami`) and PRD section 12; record accepted follow-ups.
- [x] 4.4 Run clean-checkout `make setup`, `make check`, the real-database race suite, `make smoke` and `make test-mutation` (with the documented extended budget if the default still does not fit); report handwritten and generated line counts separately; confirm no tracked source is rewritten by successful checks. Evidence 2026-09-12 at `45ccfba` plus the whole-change review fixes: `make check` passes without the database variable; `go test -race -p 1` for the database, server and CLI packages passes against PostgreSQL; `make smoke` passes including the new grant section (grant by username with a dry run first, idempotent create, member `whoami` and reads in the summary shape without targets, member denials, the counted delete guard, disabled grants visible, revoke and its effect on the next call, idempotent revoke, delete after revoke, `users block --user <username>`, grants surviving blocking, and event counts including the connection column); a clean clone runs `make setup` and `make check` (its `web/pnpm-lock.yaml` again drifts after setup, the pre-existing tooling interaction). `make test-mutation` with `CLAVIS_MUTATION_WORKERS=4 CLAVIS_MUTATION_TIMEOUT_SECONDS=3600` completes in about 1,590 s: 702 killed, 84 lived, 482 not covered, 2 timed out, source unchanged (previous change: 639 / 77 / 393 / 2; the kill ratio among covered mutants stays at 89%). The default 600 s budget still does not fit; raising it remains the owner decision recorded by the previous change. Lines since `main`: handwritten +5,074/−301 across 56 files (tests about +3,043, smoke about +300); generated (sqlc, templ) +630/−102; OpenSpec artifacts +720/−2. No tracked source was rewritten by any successful check.
- [x] 4.5 Whole-change independent review against the inherited requirements, all five delta specs and the design's inherited-decisions table, with the authorization paths end to end; record the final revision, limits and any exceptions for owner acceptance before archive. Reviewed 2026-09-12 at `45ccfba`: ACCEPT WITH FOLLOW-UPS, no blocking findings; every delta scenario mapped to a test except the `whoami` response-size boundary, which was the one should-fix: a member with up to 1,000 long-named grants could exceed the 64 KiB general response limit the CLI reads `whoami` under, so the server now keeps the names inside a byte budget with explicit truncation, with a test at the boundary. Also applied: the enabled-and-ungranted delete hint states that no grants remain; a comment on the connection read routes describes the real order of role checks; the README describes `CONNECTION_DISABLED` as the authorization check's refusal rather than a shipped operation. Accepted as is: administrators' `whoami` omits the `connections` key rather than sending an empty array (all layers agree); the change is larger than the design's estimate (about 2,000 non-test handwritten lines against 700 to 900, most of it the username-addressing rework and the member read methods). Verified end to end: no code path hands a member a full connection record; grant events carry both parties; the widened user lock introduces no lock-order cycle. Ready for archive once the owner has checked the commits.
