Four slices in dependency order; implementation and independent acceptance review are separate tasks, and a reviewer never approves their own implementation. Slice 1 lands first; slices 2 and 3 depend on it and can run in parallel (slice 3 against a fake executor); slice 4 needs everything. Expect roughly 900 to 1,200 changed handwritten lines; generated sqlc output reported separately.

## 1. Contracts, migration and the PostgreSQL execute operation

Requirements: query-execution "Pass-through execution request" (session settings, connection lifecycle), "Structured results", "Bounds and explicit truncation", "Distinguishable failures" (mapping); connection-management "Provider registry" (execute operation, unsupported); local-authentication "Secret-free authentication events" (allowlists, metric columns, migration). Owned paths: `internal/auth/query.go` (DTOs, `QueryExecutor`, codes, action, outcomes, bounds), `internal/auth/errors.go` (`Source`), `internal/auth/events.go` (metrics), `internal/provider/provider.go` (`Execute`, typed errors), `internal/provider/postgres.go` and `postgres_test.go` (execution with streaming drain, real-PostgreSQL tests), `internal/provider/victoriametrics.go` (unsupported), `internal/database/migrations/005_query_events.sql`, `internal/database/queries/auth.sql` and `initialize.sql`, `internal/database/sqlc/`, `internal/database/events.go` and `auth.go` (metric parameters), `internal/database/initialize.go`, related `_test.go`. Verify: `make generate-db && make check-db-generated && make check-sql-boundaries && go test -race ./internal/auth/ && CLAVIS_BACKEND_TEST_DATABASE_URL=... go test -race -p 1 ./internal/provider/ ./internal/database/ -run 'Migrat|Generated|Event|Execute|Query'`.

- [ ] 1.1 Add the query DTOs, `QueryExecutor`, the five codes with their statuses and `error.source` rendering, `query.execute` and the five outcomes, the SQL bound and envelope allowance, and the event metric fields; verify allowlist tables, `Event.Valid`, JSON shapes (`null` values, list-always) and redaction.
- [ ] 1.2 Add `Execute` to the provider interface with typed errors; implement PostgreSQL execution over the simple protocol with the session settings, type-name resolution, streaming drain with row and byte caps and the first-row rule, command tags, the statement index on source errors, timeout and connect-failure mapping, and the bounded close; VictoriaMetrics returns unsupported. Real-PostgreSQL tests: single and multi-statement scripts, implicit transaction rollback on a failing statement, typed values as text, duplicate column names, statements without rows, row cap and byte cap truncation with drain (writes after the cap still commit), `MaxRows` lowering, `statement_timeout` producing the timeout error, wrong password and unreachable host mapping, the secret never appearing in errors, unknown OID rendering, a context deadline shorter than the statement.
- [ ] 1.3 Add migration `005` (metric columns, widened allowlists), extend `InsertAuthEvent`, the event recorder and readiness; regenerate; verify fresh and initialized databases apply `005` once, existing rows keep null metrics, the allowlists accept the new entries and refuse near misses, and the CLI-only build stays Go-only.
- [ ] 1.4 Independent acceptance review of slice 1 including the drain and error-mapping paths; record the revision and generated size.

## 2. Service

Requirements: query-execution "Pass-through execution request" (authorization first, unsupported before secrets, no pool), "Audited executions"; connection-grants "Per-request connection authorization" (inherited). Depends on slice 1. Owned paths: `internal/database/query.go`, `internal/database/query_test.go`, `internal/database/auth.go` (`recordRequest`), related tests. Verify: `CLAVIS_BACKEND_TEST_DATABASE_URL=... go test -race -p 1 ./internal/database/`.

- [ ] 2.1 Implement `ExecuteQuery`: local validation with hints and no event, `AuthorizeConnection`, provider lookup and `PROVIDER_UNSUPPORTED` before the secret, secret open and wipe, `Execute` with the record's bounds and the lowered `MaxRows`, then the event with outcome and metrics in its own transaction; audit failure after execution reported as unavailable.
- [ ] 2.2 Real-PostgreSQL tests: administrator and granted member succeed; ungranted member, disabled connection, revoked session and unsupported provider are refused with the documented codes and events and without opening the secret; truncated outcome and metrics; source error, timeout, unreachable and refused-credentials outcomes with no message in the event; undecryptable secret; sentinel SQL text never enters any event; the audit write injected to fail after a successful execution; no platform transaction open during the external call (assert via `pg_stat_activity` on the platform database).
- [ ] 2.3 Independent acceptance review of slice 2; record the revision.

## 3. Route and CLI

Requirements: query-execution "Query route and command", "Distinguishable failures" (envelope); cli-authentication "Query command", "Bounded and non-disclosing authentication transport" (per-route bounds). Depends on slice 1; tests use a fake executor. Owned paths: `internal/server/query.go`, `internal/server/query_test.go`, `internal/server/auth.go` (route, rejection mapping, `HandlerWithAuth` dependency), `internal/server/server.go`, `cmd/server`, `internal/cli/query_commands.go`, `internal/cli/query_test.go`, `internal/cli/auth_transport.go` (per-route bounds, `routeFailures`, `source` decoding), `internal/cli/result.go` (rendering), `internal/cli/cli.go`, `internal/cli/*_test.go`. Verify: `go test -race ./internal/server/ ./internal/cli/ && make build-cli`.

- [ ] 3.1 Mount `POST /api/query` with the shared handler shape, the SQL body bound, strict decoding, one adapter rejection event, `error.source` in the envelope, and the defensive response-size assertion; require the executor in `HandlerWithAuth`.
- [ ] 3.2 Add the `query` command with the three SQL inputs and local validation, per-route request and response bounds in the transport, the failure allowlist, strict validation of the results document, JSON and text rendering including source errors; unit and subprocess tests for every spec scenario, plus the real-database route round trip once slice 2 lands (coordinator).
- [ ] 3.3 Independent acceptance review of slice 3; record the revision.

## 4. Smoke, documentation and whole-change verification

Requirements: all delta specs end to end; project-bootstrap quality and smoke requirements. Depends on slices 1 to 3. Owned paths: `scripts/smoke.mjs`, `README.md`, `docs/PRD.md` (section 12 status), `reports/` (ignored). Verify: `make check && make smoke && make test-mutation`.

- [ ] 4.1 Extend smoke with the query section from the design (script, truncation, `--max-rows`, syntax error, stdin and file inputs, denials, unsupported provider, timeout, event outcomes and metrics, sentinel SQL absent from events).
- [ ] 4.2 Update README (query section, bounds, failure codes, the role connection limit as the throttle, the catalog query for discovery) and PRD section 12; record accepted follow-ups (cursor optimisation, pooling).
- [ ] 4.3 Run clean-checkout `make setup`, `make check`, the real-database race suite, `make smoke` and `make test-mutation` (with the documented extended budget); report handwritten and generated line counts separately; confirm no tracked source is rewritten by successful checks.
- [ ] 4.4 Whole-change independent review against the inherited requirements, all four delta specs and the design's inherited-decisions table, with the pass-through, bounds and audit paths end to end; record the final revision, limits and any exceptions for owner acceptance before archive.
