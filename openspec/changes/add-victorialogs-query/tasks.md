Three slices in dependency order; implementation and independent acceptance review are separate tasks, and a reviewer never approves their own implementation. Slice 1 lands first; slices 2 and 3 depend on it. Expect roughly 1,000 to 1,300 changed handwritten lines; no generated output is expected.

## 1. Contracts, the provider and the shared HTTP helpers

Requirements: query-execution "Pass-through execution request" (endpoints, headers, no parsing), "Structured results" (rows as written), "Bounds and explicit truncation" (stop at the cap, one-row ceiling, discovery drain), "Distinguishable failures" (`http_<status>`, `malformed_response`); connection-management "Provider registry" (third provider, tenant settings), "Explicit connectivity check". Owned paths: `internal/auth/query.go`, `internal/auth/query_test.go`, `internal/auth/connections.go` (provider type), `internal/provider/provider.go`, `internal/provider/victorialogs.go`, `internal/provider/victorialogs_test.go`, `internal/provider/victoriametrics.go` (helpers extracted, refuse log inputs), `internal/provider/postgres.go` (refuse log inputs), `internal/provider/provider_test.go`, `internal/provider/victoriametrics_test.go`. Verify: `go test -race ./internal/auth/ ./internal/provider/` (fake HTTP servers; no real source).

- [x] 1.1 Extend the contracts: the `victorialogs` provider type, the request inputs with `limit` as an optional non-negative integer, the six result types, the tenant validation; JSON tests proving omitted versus zero `limit` and that the other providers' shapes are unchanged.
- [x] 1.2 Implement the provider: target parsing with `accountId` and `projectId` and the header-name collision refusal, the probe, endpoint selection and parameter encoding with `timeout`, the shared client and header application beside the tenant headers, the bounded line reader with the row and byte caps, the first-row rule, the one-row ceiling, stop-and-close at the cap, exact-cap-versus-cutoff, the discovery envelope reader with `value` and `hits` exact, the failure mapping; the other two providers refuse log inputs. Fake-server tests: each endpoint receives exactly the submitted parameters and headers; rows pass through with every field and value type unchanged and in order; aggregate rows; empty body; cutoff at the row cap and at the byte cap with the connection closed; exact cap without cutoff; oversized row; a non-JSON line after rows; a final line without a newline; 400 and 503 plain text; 401 and 403; redirect refused; connection refused; client deadline; the secret and tenant values never in any error; the four auth methods with and without tenant headers.
- [x] 1.3 Independent acceptance review of slice 1; record the revision. Reviewed 2026-09-14 at `da5fae6`: ACCEPT WITH FOLLOW-UPS, all applied by the coordinator in the follow-up commit: a final row without a newline was allowed one byte over the ceiling; the client deadline reached while rows are being read is now tested; byte accounting is asserted for non-string values and for discovery items; a mislabelled malformed case was renamed and a swapped assertion fixed. Accepted deviations: `timeout` sent as a duration string (`30s`), which the source parses; tenant headers canonicalised by Go on the wire, which the source reads case-insensitively; a discovery item without `value` or `hits` is malformed; the filter bound is enforced by the service in slice 2. Noted for slice 3: a plain-text error line arriving right after the cap is `malformed_response`, not truncation, because the reader parses the next line before deciding. Size: +2,489/−454 handwritten, above the change's estimate, 43% tests and 461 lines of helpers extracted from the metrics provider without behaviour change; no generated output.

## 2. Service, route and CLI

Requirements: query-execution "Pass-through execution request" (input rules, provider mismatch before the secret), "Query route and command"; cli-authentication "Query command", "Connection management commands" (`--account-id`, `--project-id`). Depends on slice 1. Owned paths: `internal/database/query.go`, `internal/database/query_test.go`, `internal/server/query.go`, `internal/server/query_test.go`, `internal/cli/query_commands.go`, `internal/cli/query_test.go`, `internal/cli/connections_commands.go`, `internal/cli/auth_transport.go`, `internal/cli/result.go`, `internal/cli/*_test.go`. Verify: `go test -race ./internal/server/ ./internal/cli/ && make build-cli`, and `CLAVIS_BACKEND_TEST_DATABASE_URL=... go test -race -p 1 ./internal/database/ ./internal/server/` (the log source is a fake HTTP server in the real-database tests).

- [x] 2.1 Service and route: eleven-input exclusivity, the limit, filter and match rules with hints, the provider-input match for `victorialogs` after authorization and before the secret, `limit` decoded as a JSON integer; real-database tests with a fake log source for a granted member, refusals, mismatch, truncation with the hint, the source's 503 and the platform's timeout.
- [x] 2.2 CLI: the new query flags and local validation, the connection tenant flags, response validation by `provider` for the six result types, text rendering with quoting, `error.source` for a log failure; unit and subprocess tests for every spec scenario.
- [x] 2.3 Independent acceptance review of slice 2; record the revision. Reviewed 2026-09-14 at `ea6f857`: ACCEPT WITH FOLLOW-UPS, all applied by the coordinator in the follow-up commit: the setting-key grammar the CLI now uses for target keys is tested directly; migration 006 is tested on an installation that already stores connections (rows survive, the third provider is accepted afterwards, an unknown one still refused); the time-flag hint names the log discovery inputs; the shared `SOURCE_ERROR` headline reads "The source rejected the query" instead of naming SQL. Accepted deviation: a truncated success carries no hint (the envelope has none), so the spec scenario and design now say the guidance lives in the README, the ceiling hint and the skill. Slice 2 also found and fixed two gaps outside its brief: migration 003 pinned the provider check to two names, so migration 006 replaces it; the CLI validated target keys with the label grammar, which refused `accountId`. Handwritten +1,620/−83 (65% tests), no generated output.

## 3. Compose, smoke, documentation and whole-change verification

Requirements: all three delta specs end to end; project-bootstrap quality and smoke requirements. Depends on slices 1 and 2. Owned paths: `compose.yaml`, `scripts/smoke.mjs`, `README.md`, `docs/PRD.md` (sections 7.4, 9 and 12), `reports/` (ignored). Verify: `make check && make smoke && make test-mutation-full`.

- [x] 3.1 Add the pinned single-node VictoriaLogs service to compose with the short maximum query duration and a health check; extend smoke with the log section from the design, ingestion anchored inside retention and polled until visible. Done 2026-09-14: `victoriametrics/victoria-logs:v1.52.0` pinned by digest with `-search.maxQueryDuration=5s` and `-retentionPeriod=1d`; the image ships no shell or wget, so the container has no health check and smoke waits on `/health` over HTTP; smoke ingests six rows with the `application/stream+json` content type (a form content type is silently ignored by the source), polls until a query sees them, then proves the ordered query under a source limit, the cut at `--max-rows` without one, a stats aggregate, the five discovery endpoints with `--filter` and `--start`, text output with a two-line message on one line, the source's `http_400`, local refusals of a limit on field names and of discovery without `--match`, provider mismatch in every direction, member refusal then grant, the tenant and bearer headers observed by a recording stub on both the check and the query, and the stalling stub as `SOURCE_TIMEOUT`.
- [x] 3.2 Update README (log queries, discovery, `--limit` versus `--max-rows`, ordering through a sort pipe, truncation meaning, tenant settings, hit counts under a limit) and PRD sections 7.4, 9 and 12 with the scope decision of 2026-09-14. Done 2026-09-14: README gains the VictoriaLogs subsection (tenant settings, `--limit` versus `--max-rows`, ordering through a sort pipe, the truncation meaning, discovery with hit counts under a limit, the error line after rows) and names three providers; PRD 7.4 gains the VictoriaLogs block, 9 lists it in the MVP and moves Loki to later stages, 12 records the decision, the pilot target and the status line.
- [x] 3.3 Run clean-checkout `make setup`, `make check`, the real-database race suite, `make smoke` and `make test-mutation-full`; report handwritten and generated line counts separately; confirm no tracked source is rewritten by successful checks. Evidence 2026-09-14 at `0a85f77`: `make check` passes without the database variable and rewrites nothing; `go test -race -p 1` for auth, provider, database, server and CLI passes against PostgreSQL (the session-expiry timestamp test failed once while the laptop slept and the container clock drifted, and passed with the machine held awake); `make smoke` passes against the pinned VictoriaLogs (a first run failed at the metrics import with the source's 503 because the Docker data disk was full; the owner approved pruning the build cache, 17 GB, and the rerun passed); a clean clone at `41cc316` runs `make setup` and `make check` (the pre-existing lockfile drift only). `make test-mutation-full` with the default eight workers completes in about 1,680 s: 938 killed, 115 lived, 615 not covered, 2 timed out, source unchanged (previous change 863 / 110 / 566 / 2; 89% of covered mutants killed). Lines since `main`: handwritten +5,100/−558 across 41 files (tests +2,402/−24; smoke, compose and docs +421/−7; OpenSpec +450/−0; one 8-line migration), no generated output. No tracked source was rewritten by any successful check.
- [x] 3.4 Whole-change independent review against the inherited requirements, all three delta specs and the design's inherited-decisions table, with the pass-through, cutoff, ceiling and failure paths end to end; record the final revision, limits and any exceptions for owner acceptance before archive. Reviewed 2026-09-14 at `41cc316`: ACCEPT WITH FOLLOW-UPS, none blocking, all applied before archive: the README quoting rule now lists backslashes, `=` and empty values; two smoke comments damaged by a rename were restored; a tenant setting on a PostgreSQL connection is tested at the service (refused with the allowed-settings hint). Exception accepted by the coordinator on the owner's behalf: once the cap is full the reader reads one more line to learn whether the stream ended, so an exact-cap answer that ended cleanly reports `truncated: false`, while a row beyond the ceiling or a source stalling at that point is reported as that failure rather than as a truncated answer; the README says so. Coverage table confirmed every delta scenario has a test or smoke assertion. Verified end to end: nothing parsed, sorted, defaulted or added beyond `timeout` and no synthesized limit on the log path; six constant endpoints; authorization, then the provider-input match, then the executor assertion, then the secret; tenant headers beside the stored auth with the collision refused; no secret or tenant value in any error; the client deadline, service backstop and request budget layered; stop-and-close at both caps with the per-row ceiling; discovery envelopes drained; rows byte-exact in arrival order; CLI shape validation per result type; the failure mapping without any text search; migration 006 safe for existing rows; docs, compose and the recorded departures accurate. Limits for the owner: a single row larger than what the CLI reads is `INVALID_RESPONSE` at the CLI; discovery hit counts under a source limit are not observed; the source's own timeout is its `503`.
