Four slices in dependency order; implementation and independent acceptance review are separate tasks, and a reviewer never approves their own implementation. Slice 1 lands first and leaves a green tree: it carries the mechanical recipient migration through every consumer of the grant contract, with the new interfaces declared but nothing wired to them; slices 2 and 3 depend on it and can run in parallel against fakes; slice 4 needs everything. Estimates, flagged in the proposal as well above the rescoping signal: production code 1,400 to 2,000 handwritten lines, tests and smoke 2,500 to 3,200, regenerated sqlc and templ output 500 to 800, each reported separately (the archived grants change recorded about 2,000 production lines and +5,074 handwritten in total; this one avoids its username-reference rework but adds a whole command group and a table rewrite). Run every Go suite that includes the database-backed packages under `caffeinate`, against the test database on port 5432.

## 1. Contracts, migration and queries

Requirements: group-management "Group records" (shape, grammar, counts), "Group membership" (shape); connection-grants "Grant records" (recipient), "Member visibility of granted connections" and "Per-request connection authorization" (effective-access queries), "Grant routes and listing" (effective entries, `whoami` groups). Owned paths: `internal/auth/groups.go` (new), `internal/auth/grants.go` (recipient, filter, access types, `ListEffectiveAccess`), `internal/auth/contracts.go` (`Identity` groups), `internal/auth/errors.go`, `internal/auth/validation.go` or `password.go` (`ValidGroupName`, `ValidGroupRef`), `internal/database/migrations/007_groups.sql`, `internal/database/queries/groups.sql` (new), `queries/grants.sql` (nullable recipient, group filter, effective-access predicate, effective listing, group names), `queries/initialize.sql` (column checks), `internal/database/sqlc/`, `internal/database/initialize.go`, related `_test.go`. Verify: `make generate-db && make check-db-generated && make check-sql-boundaries && go test -race ./internal/auth/ ./internal/database/ -run 'Migrat|Generated|Group|Grant|Ref|Name'`.

- [ ] 1.1 Add the group, membership, recipient and access DTOs, the `Groups` interface, `ListEffectiveAccess` on `Grants`, `Identity.Groups`, `ValidGroupName` and `ValidGroupRef`, the codes `GROUP_NOT_FOUND`, `GROUP_EXISTS` and `GROUP_IN_USE` with hints, the route paths and listing bounds; unit tests for the validators, the recipient rule (exactly one) and the code table.
- [ ] 1.2 Add migration `007` (groups with the character-count description bound, group_members with cascade and the `(user_id, group_id)` index, the grants rewrite with the one-recipient check and the two partial unique indexes) and the named queries (group lifecycle and locks, membership, counts, group filter on the grant listing, the effective-access predicate in the four member queries, the effective listing union, group names); extend the readiness column checks; regenerate; verify fresh and initialized databases apply `007` once, existing grant rows survive, both partial indexes enforce uniqueness, the check refuses zero or two recipients, cascade removes memberships, the member queries return a connection once through two paths, and the CLI-only build stays Go-only.
- [ ] 1.3 Carry the recipient migration through every consumer so the tree compiles and the existing suites pass with group behaviour unimplemented: `grants.go` resolver signatures and the user-only recipient path, the server grant adapters and their fakes, the CLI grant validators and renderers, the web grants table, and every test fixture that names `grant.user`; the `Groups` interface and `ListEffectiveAccess` stay declared but unwired, with no placeholder implementation. Verify `make check` and the real-database grant suites still pass.
- [ ] 1.4 Independent acceptance review of slice 1; record the revision and generated size.

## 2. Service: groups, membership, recipients and effective access

Requirements: group-management "Group records", "Group membership", "Administrator-only group operations and guarded delete"; connection-grants "Grant records", "Member visibility of granted connections", "Per-request connection authorization", "Grant routes and listing" (service half: filters, effective listing, `whoami` names). Depends on slice 1. Owned paths: `internal/database/groups.go` (new), `internal/database/groups_test.go` (new), `internal/database/grants.go` (recipient resolution and group row lock, group filter, `ListEffectiveAccess`, `ListGroupNames`, member reads on the new queries), `internal/database/grants_test.go`, `internal/database/connections.go` (delete guard unchanged, verified), related tests. Verify: `CLAVIS_BACKEND_TEST_DATABASE_URL=... caffeinate go test -race -p 1 ./internal/database/`.

- [ ] 2.1 Implement `auth.Groups` on `LocalAuth` through `administer` with the sixth advisory key, `LockGroup` by UUID or name, the user reference as `targetRef` for membership mutations, the three group codes added to the `denial` allowlist with their hints re-attached as `DeleteConnection` does, `GROUP_EXISTS` on create and rename collisions with nothing written, the character-count description validator, the guarded delete with the count in the hint on real and dry runs, idempotent add and remove including a blocked user, and the administrator read transaction for list, get and members with counts.
- [ ] 2.2 Rework grants: resolve the recipient by kind and lock the group row after the user rows inside the grant transaction, add the group filter with the member `FORBIDDEN` before lookup, implement `ListEffectiveAccess` with the server-side subject rule (members default to self, other users refused, administrators must name one) returning the subject's record, `ListGroupNames`, and switch the member read paths and `AuthorizeConnection` to the effective-access queries.
- [ ] 2.3 Real-PostgreSQL tests: group create, get by UUID and by name, duplicate and UUID-shaped names, a description of exactly 2,000 multibyte characters accepted and 2,001 refused, description cleared on update, update of each field and a rename that keeps members and grants, rename collision with no mutation, delete guard with the count on real and dry runs, delete removing memberships, delete then recreate with the same name inherits nothing, unknown references, member denials for every operation, dry runs leaving no rows; membership add and list, idempotent add and remove, adding a blocked user, blocked member keeps membership; group grant create and revoke, both-or-neither recipient, distinct direct and group grants on one connection, a user and a group with the same name holding distinct grants; member listing and get through a group only, once through two paths, absent after leaving the group or after the group's grant is revoked; authorization through a group, through direct-plus-group and two groups with one path removed, after the last path is removed, on a disabled connection; effective listing with three paths and after revoking one, an administrator subject with group grants before and after demotion, a member naming themselves by UUID and by username, a member naming another user, an administrator omitting the user; `whoami` names through a group; concurrent group delete and group grant serialize in both orders with different actors (`GROUP_IN_USE` or `GROUP_NOT_FOUND`); membership add against a block of the same user; held-lock timeout.
- [ ] 2.4 Independent acceptance review of slice 2 including the authorization paths and the recorded lock order; record `EXPLAIN (ANALYZE, BUFFERS)` for the point lookup, the member listing and the name list on a selective member and on a heavily granted connection, and the revision.

## 3. Routes and CLI

Requirements: group-management "Group routes"; connection-grants "Grant routes and listing"; cli-authentication "Group management commands", "Grant commands", "Authoritative identity and explicit sign-out". Depends on slice 1; tests use fakes. Owned paths: `internal/server/groups.go` (new), `internal/server/groups_test.go` (new), `internal/server/grants.go` (recipient decoding, group filter, effective route), `internal/server/auth.go` (routes, rejection mapping, `identityJSON` groups and the shared byte budget, `HandlerWithAuth` dependency), `internal/server/*_test.go`, `internal/cli/groups_commands.go` (new), `internal/cli/groups_test.go` (new), `internal/cli/grants_commands.go`, `internal/cli/auth_commands.go` (`whoami` rendering), `internal/cli/auth_transport.go`, `internal/cli/result.go`, `internal/cli/*_test.go`. Verify: `go test -race ./internal/server/ ./internal/cli/ && make build-cli`, then `CLAVIS_BACKEND_TEST_DATABASE_URL=... caffeinate go test -race -p 1 ./internal/server/` once slice 2 has landed.

- [ ] 3.1 Mount the eight group routes and the effective listing with the shared handler shape, decode the grant recipient with the exactly-one rule and its hint, add the group filter, pass the group, member and effective listings through `boundedListing` under `MaxListingBody`, extend `identityJSON` with the group names under the 8 KiB reservation and independent truncation flags, and require the `Groups` dependency in `HandlerWithAuth`; tests for a legitimate listing above 64 KiB, truncation at the listing ceiling, the `whoami` byte boundary with groups reserved and the reservation reclaimed, empty lists, and an administrator identity.
- [ ] 3.2 Add the `groups` CLI group with local validation and text renderings, `--group` and `--effective` on the grant commands with the exclusivity rules and the subject sent as given, the recipient and subject rendering with strict validation of `source` and `group` combinations, `whoami` groups, `responseLimit` extended to the new listings, and the per-route error allowlists; tests for every spec scenario in unit and subprocess form, plus real-database route round trips for a group grant, effective access and the guarded delete.
- [ ] 3.3 Independent acceptance review of slice 3; record the revision.

## 4. Web, skill, smoke, documentation and whole-change verification

Requirements: group-management "Read-only groups table in the browser"; connection-grants "Read-only grants table in the browser"; agent-skill "Embedded skill content" (line bound, no access instruction) and "Skill cannot drift from the CLI"; all delta specs end to end; project-bootstrap quality and smoke requirements. Depends on slices 1 to 3. Owned paths: `internal/web/auth.templ`, `internal/web/auth_models.go`, `internal/web/*_test.go`, regenerated `*_templ.go`, `internal/server/auth.go` (`adminPage` groups load, `/admin/groups`), `internal/server/*_page_test.go`, `internal/skill/clavis/SKILL.md`, `internal/cli/skill_drift_test.go`, `scripts/smoke.mjs`, `README.md`, `docs/PRD.md` (sections 7.2, 9, 12), `reports/` (ignored). Verify: `make generate-web && make check && make smoke && make test-mutation-full` (detached, under `caffeinate`; the full scope is the pre-archive requirement, the diff-scoped run is for iteration).

- [ ] 4.1 Add the Groups page and sidebar entry, the recipient column with the group badge on the Grants page, load groups in `adminPage` failing closed, with rendering and page tests including the escaped description and the truncation notice.
- [ ] 4.2 Add `groups` to the skill's administration vocabulary, the sentence on group-derived access and `whoami` groups, and `grants list --effective` as the way to learn which path supplies a connection; keep the entry under 200 lines; confirm the drift test covers the new commands and flags.
- [ ] 4.3 Extend smoke with the group flow from the design (group grant inherited, listed once beside a direct grant, effective paths, direct revoke leaves access, membership removal ends it on the next call, guarded delete with the count, delete after revoke, member `groups list` refused).
- [ ] 4.4 Update README (Groups section, grants recipients and `--effective`, `whoami` groups, the planned-work sentence) and PRD sections 7.2, 9 and 12; record accepted follow-ups.
- [ ] 4.5 Run clean-checkout `make setup`, `make check`, the real-database race suite under `caffeinate`, `make smoke` and `make test-mutation-full`; report production, test and generated line counts separately against the estimates; confirm no tracked source is rewritten by successful checks.
- [ ] 4.6 Whole-change independent review against the inherited requirements, all three delta specs and the design's inherited-decisions table, with the effective-access paths end to end (inheritance, provenance, revocation through each path, the delete guard); record the final revision, limits and any exceptions for owner acceptance before archive.
