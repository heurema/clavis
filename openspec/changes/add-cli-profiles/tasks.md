Five slices plus integration and release. Each slice has an implementation task and a separate independent acceptance review; a reviewer never approves their own implementation. Every slice leaves a green tree, including the dead-code check, so nothing is added before something calls it. Slices run in order: 2 needs the home from 1, 3 needs the target from 2, 4 needs the configuration writer's reader and the storage reader from 1 and 2, 5 documents all of them.

Estimates (handwritten production / tests): slice 1 70–110 / 70–110; slice 2 150–200 / 150–220 plus a sweep of tests that reach servers through `CLAVIS_SERVER_URL` (four test files, about ten sites); slice 3 40–60 / 40–80 plus a sweep of exact text expectations; slice 4 180–240 / 180–260; slice 5 SKILL.md 20–35, drift test 15–30, README 40–60, PRD 2, smoke 30–50. Slices 3 and 4 cross the six-file rescoping signal: slice 3 because the first text line touches every text-output test file (the sweep goes through one helper and is reported separately), slice 4 because the five commands, their writer and their renderers are one coupled behaviour. Neither is split further. No database is needed except for the integration checks.

Test isolation for every slice: the `cliHome` helper in `internal/cli/auth_test.go` and the subprocess environment in `subprocess_posix_test.go` set `HOME` and clear `CLAVIS_HOME`, `CLAVIS_PROFILE` and `CLAVIS_SERVER_URL` before setting what the test needs, so a developer's own configuration never leaks in. `CLAVIS_SERVER_URL` stays cleared after slice 2 removes its readers, so a regression that reads it again cannot pass on a developer machine that still exports it.

## 1. Clavis home and session location

Requirements: cli-authentication "Origin-bound protected session storage" (delta: all scenarios). Design decision 1. Owned paths: `internal/cli/home.go` (new, `clavisHome`, cross-platform), `internal/cli/cache_posix.go`, `internal/cli/auth_commands.go` (typed storage error rendering), `internal/cli/cache_posix_test.go`, `internal/cli/auth_test.go` (`cliHome`), `internal/cli/subprocess_posix_test.go`, `internal/cli/skill_test.go`. Verify: `go test -race ./internal/cli/`; `make build-cli`, then `h=$(cd "$(mktemp -d)" && pwd -P); HOME=$h ./bin/clavis logout --server http://127.0.0.1:1` (resolved, because macOS `mktemp -d` is under the `/var` symlink) exits 0 and creates nothing outside `$h/.clavis/sessions` (mode 0700), with nothing under `$h/Library` or `$h/.config`; `make check`.

- [x] 1.1 Add `clavisHome()`; point `openCache` at it per decision 1: keep the existing `O_NOFOLLOW` walk, apply the non-exact check to the home and the exact 0700 check to `sessions`; drop `os.UserConfigDir()` and the `clavis` component. Return `storageError{path, requirement}` and render it in `runAuth`; a relative `CLAVIS_HOME` returns a typed error here and gets its `INVALID_ARGUMENT` mapping in slice 2's resolution. Tests: 0755 home, group-writable home and symlinked home refused with the path in the message, `sessions` at 0755 or as a symlink refused, a `CLAVIS_HOME` whose missing components are created owner-only, relative `CLAVIS_HOME`, two origins stored independently, and a session file in the former `os.UserConfigDir()` location ignored. Update `cliHome`, the subprocess offline test (make the new `sessions` unsafe) and the skill test's "no configuration directory created" assertion to the new path.
- [x] 1.2 Independent acceptance review of slice 1 against the storage scenarios: run the verifications, confirm by reading `cache_posix.go` that the walk still refuses symlinks on every component and nothing below `sessions` changed (digest names, locks, atomic write, token comparison on remove), record the revision and line counts. Reviewed at `716d8de`: ACCEPT WITH FOLLOW-UPS, no blocking findings. The reviewer reran the race suite uncached, the logout check in a resolved temporary home and `make check`, confirmed each storage scenario against the code (the shared-session scenario waits for profiles), and killed five of six hand mutants of the storage guards. Applied in `eb98037`: a test for a root-owned home such as `/private/tmp`, because deleting the home's ownership check had survived every test; an unset `HOME` now reads "must be set to an absolute path"; a comment on the helper that unsets the variables it clears. Moved to slice 5: the smoke and kind scripts pass a developer's `CLAVIS_HOME` through to every client. Accepted as is: a home that fails the walk is reported with its full rule rather than a mode number, since it need not be exactly 0700; lock and session-file failures below `sessions` keep the generic message; a directory the walk cannot create is reported as the ownership rule without the system error. Production +80/−32, tests +159/−11.

## 2. Configuration reading, server resolution and doctor

Requirements: cli-authentication "Server resolution" (all scenarios; the `server` and `profile` fields they imply are verified in slice 3), "Client profiles" (the file rules, "Corrupt configuration", "Login never writes the configuration"), project-bootstrap "Structured CLI diagnostics" (delta: "Reject invalid arguments", "Diagnose through the current profile" without the envelope assertion). Design decisions 2 (reading only), 3, 5 and 7. Depends on slice 1. Owned paths: `internal/cli/config.go` (new), `internal/cli/config_test.go` (new), `internal/cli/auth_commands.go` (flags, resolution call), `internal/cli/cli.go` (`doctor` flags), `internal/cli/doctor.go`, `internal/cli/cli_test.go`, `go.mod`, `go.sum`, and the `CLAVIS_SERVER_URL` sites in `auth_test.go`, `subprocess_posix_test.go` and `skill_test.go`. Verify: `go test -race ./internal/cli/`; `make check`; with a scratch `CLAVIS_HOME` holding a hand-written file, `./bin/clavis whoami` goes to the current profile's server, `--profile` and `CLAVIS_PROFILE` override it, `--server` with `--profile` exits 2, `CLAVIS_SERVER_URL` alone exits 2 with the `profiles set` hint, and an empty home exits 2 with no connection attempt (checked with a listener that records accepts on `127.0.0.1:8080`).

- [x] 2.1 Add `github.com/pelletier/go-toml/v2` at its latest release. Implement `loadConfig` (64 KiB bound, strict decode, validation of names with a profile-specific message, servers and `current`, `INVALID_ARGUMENT` naming file and key) and `resolveTarget` per decision 3. Remove the default and `Sources` from `--server`, add `--profile` to every networked command and to `doctor`, call resolution first in `runAuth` and in the `doctor` action, move the relative `CLAVIS_HOME` check into it, replace `validateURL` with `auth.CanonicalOrigin` and delete it. Move the tests that reached servers through `CLAVIS_SERVER_URL` onto `--server`. Tests: a table over flag, environment and `current` combinations including an empty `CLAVIS_PROFILE`, `--server` with `--profile`, `CLAVIS_SERVER_URL` ignored, unknown profile from each source, absent file, unparseable file, unknown top-level and profile keys, invalid name, invalid server, dangling `current`, a direct server with a corrupt file, `login --profile` with a corrupt file failing before stdin is read, relative `CLAVIS_HOME` failing before stdin is read, `login` leaving the file untouched, and `doctor` refusing a base path and non-loopback HTTP; confirm no password is read and no cache is opened when resolution fails.
- [x] 2.2 Independent acceptance review of slice 2 against the resolution and diagnostics scenarios: run the verifications, confirm no networked command reaches a server without a resolved target (walk the command tree the drift test walks), confirm `CLAVIS_SERVER_URL` appears nowhere in production code, confirm offline commands never call `loadConfig`, confirm the dependency is the only new module, record the revision and line counts with the test sweep reported separately. Reviewed at `a0857e6`: ACCEPT WITH FOLLOW-UPS, no blocking findings. The reviewer reran the race suite uncached and `make check`, drove the binary against two local listeners and a hand-written file through every resolution source, saw no accept on `127.0.0.1:8080` from an empty home, confirmed a FIFO, directory or symlinked `config.toml` is refused without hanging, walked the command tree to confirm every networked command resolves before input, storage or network, and killed all eight hand mutants of the resolution and file guards; a ninth showed the argument-validation tests no longer pass on the "no server" error for the wrong reason, which the sweep had found in 22 of them. Applied in `46d21e3`: a set but empty `--server` or `--profile` is refused rather than treated as absent; keys must match `current`, `profiles` and `server` exactly, since the decoder folds case; the non-regular file message says "must be a regular file". Spec updated: the 64 KiB bound, exact keys, empty flags and the malformed-name rule. Accepted as is: `resolveTarget` reads the environment and the file directly rather than through injected functions; `openCache` still derives the home; a syntax error reports only line and key because the decoder's message quotes the document; a UTF-8 byte order mark is invalid TOML. Production +262/−33, new tests about +420, mechanical sweep +71/−57.

## 3. Server and profile in every networked result

Requirements: cli-authentication "Authentication preserves CLI output conventions" (delta: all scenarios), and the envelope assertions deferred from slice 2 ("Diagnose a working environment", "Diagnose through the current profile"). Design decision 4. Depends on slice 2. Owned paths: `internal/cli/result.go`, `internal/cli/auth_commands.go`, `internal/cli/cli.go`, `internal/cli/io_test.go` (helper), `internal/cli/config_test.go` (the field assertions deferred from slice 2), and the exact text expectations in `auth_test.go`, `users_test.go`, `connections_test.go`, `grants_test.go`, `groups_test.go`, `query_test.go`, `subprocess_posix_test.go`. Verify: `go test -race ./internal/cli/`; `make check`; `./bin/clavis whoami --server http://127.0.0.1:1` prints `server` and `profile: ""` on `SERVER_UNREACHABLE`; `--output text` prints the server first, before the error line.

- [x] 3.1 Add the `Server` and `Profile` pointer fields after `ok`, set them through one helper after resolution in `runAuth` and `doctor`, and print `Server: <origin>` or `Server: <origin> (profile <name>)` first in text when set. Add a test helper that asserts and strips that first line, and move the exact text expectations onto it. New tests: success and failure carry both fields, a one-off has `profile: ""`, a result failing before resolution has neither, `version` and a `skill` refusal have neither (`skill show` success prints the bare document without an envelope), argument validation failing after resolution carries both and still renders JSON.
- [x] 3.2 Independent acceptance review of slice 3: run the verifications, confirm every return after resolution goes through the helper (argument validation, storage failure, `UNAUTHENTICATED` without a session), confirm the query text rendering below the first line is byte-identical to before, report the mechanical sweep's size separately from the new tests, record the revision and line counts. Reviewed at `d089b76`: ACCEPT WITH FOLLOW-UPS, no blocking findings. The reviewer reran the race suite uncached and `make check`, confirmed on the binary that a one-off server reports `profile: ""`, that `https://Clavis.Example.com:443/` is reported as its canonical origin, and that resolution failures, flag conflicts, usage errors, `version`, a `skill` refusal and help carry neither field; confirmed by construction that every return after resolution passes through one wrapper, since `runAuth` only resolves and wraps `runResolved`; compared query text for a PostgreSQL table, a metrics vector and matrix and log rows between `868e4d6` and `d089b76` and found them byte-identical below the server line; and killed all five hand mutants of the fields and the text line. Follow-ups were bookkeeping only: `config_test.go` added to this slice's owned paths and the `skill show` wording corrected. Accepted as is: `doctor`'s own non-positive timeout failure now carries both fields, as it happens after resolution; one terminal subtest keeps a suffix check instead of the text helper. Production +40/−6, new tests +120/−7, mechanical sweep +38/−31.

## 4. Profiles commands

Requirements: cli-authentication "Client profiles" (all scenarios not closed in slice 2). Design decisions 1 (read-only store variant), 2 (writing) and 6. Depends on slices 1–3. Owned paths: `internal/cli/config_posix.go` (new, writer under lock), `internal/cli/profiles_commands.go` (new), `internal/cli/profiles_test.go` (new), `internal/cli/cache_posix.go` (read-only store variant, added here so the dead-code check sees a caller), `internal/cli/result.go` (renderers), `internal/cli/cli.go` (registration, positional allowance). Verify: `go test -race ./internal/cli/`; `make check`; by hand against `make dev` in a scratch `CLAVIS_HOME`: `profiles set local --server http://127.0.0.1:<port>` becomes current, `login`, `profiles list --output text`, `whoami` with no flag, a second `profiles set` leaving current alone, `profiles use`, `CLAVIS_PROFILE` override note, `profiles current` in both sources, `profiles remove` of the current profile clearing it and leaving the session file, then `whoami` exiting 2 with the hint.

- [ ] 4.1 Implement `saveConfig` (lock `config.lock` with `openLock` under the five-second deadline, re-read and validate, temp file 0600 with `O_EXCL|O_NOFOLLOW`, fsync, rename; refuse a symlinked or nonregular `config.toml`). Implement `profiles set|use|current|list|remove` per decision 6 with the positional allowance keyed on the command lineage for `set`, `use` and `remove` only, exactly one argument, the JSON shapes and the text renderers, reading sessions through the read-only store variant. Tests: every "Client profiles" scenario, concurrent `profiles set` for two names both landing in the file, a positional argument on `list` or `current` refused, an unknown name for `use` and `remove`, an unknown `CLAVIS_PROFILE` for `current` (exit 2) and for `use` and `list` (reported with the "names no profile" note), a malformed `CLAVIS_PROFILE` refused without being echoed, a symlinked `config.toml` refused for reading and writing, `profiles list` with an expired stored session, with unsafe storage and with no home directory (nothing created), and that no `profiles` command opens a network connection or carries `server`/`profile`.
- [ ] 4.2 Independent acceptance review of slice 4: run the verifications including the manual flow, confirm `set` and `remove` never open the session store, confirm `use` reads sessions before writing, confirm no token reaches any result, compare each command's behaviour with the kubectl counterpart named in decision 6 and the two recorded departures, record the revision and line counts.

## 5. Skill, documentation and smoke

Requirements: agent-skill "Embedded skill content" (delta: all scenarios). Design decision 8. Depends on slice 4. Owned paths: `internal/skill/clavis/SKILL.md`, `internal/cli/skill_drift_test.go` (positional arguments for `profiles set|use|remove`, allowlist if a hint is quoted), `README.md`, `docs/PRD.md` ("CLI profiles" row), `scripts/smoke.mjs`, `scripts/smoke-image.mjs`, `scripts/verify-kind.mjs`. Verify: `go test ./internal/cli/ -run Skill`; `wc -l internal/skill/clavis/SKILL.md` under 200; `make check`; `make smoke` (database required; run detached under `caffeinate`).

- [ ] 5.1 Rewrite the skill's setup section per decision 8 and fix the sign-in credential channel sentence. Teach the drift test's parser the three positional commands, keeping a misspelled subcommand a failure. README and PRD per decision 8. `smoke.mjs`, `smoke-image.mjs` and `verify-kind.mjs`: remove `CLAVIS_HOME` and `CLAVIS_PROFILE` from each client's inherited environment and drop the now inert `XDG_CONFIG_HOME`, so a developer's exported home cannot make clients share one session store. `smoke.mjs`: one client runs `profiles set` (becoming current), `login` and `whoami` and `doctor` with no `--server`, checks `server` and `profile` in the envelope, adds a second profile, runs `profiles list`, `use`, `current` with and without `CLAVIS_PROFILE`, and removes the current profile, then sees the setup hint.
- [ ] 5.2 Independent acceptance review of slice 5 against the skill scenarios: read the setup section as an agent would, confirm it never instructs `profiles set`, `use`, `remove` or `login` on the person's behalf, confirm the drift test passes and fails on a renamed `--profile` or a misspelled `profiles` subcommand, run the smoke, record the revision and line counts.

## 6. Whole-change integration and release

- [ ] 6.1 On the merged branch: `make check`, the database suites, `make smoke`, `make smoke-image` and the diff-scoped mutation run (`make test-mutation`), all detached under `caffeinate`; free Colima disk space before the image run. Run `openspec validate add-cli-profiles --strict`. Record results and surviving mutants with a disposition.
- [ ] 6.2 After merge, push `v0.1.0-rc.6` from `main` and watch the release run. Prepend to the generated release notes the migration text in the design's Migration Plan. Verify `go install github.com/heurema/clavis/cmd/clavis@v0.1.0-rc.6` reports the tag and a clean-home `whoami` exits 2 with the `profiles set` hint. Record the run URL in this file before archiving.
