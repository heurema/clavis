Two slices in dependency order; implementation and independent acceptance review are separate tasks, and a reviewer never approves their own implementation. Expect roughly 900 to 1,200 changed handwritten lines including the skill prose; no generated output is expected.

## 1. Skill commands, embedding and the drift guard

Requirements: agent-skill "Skill install command", "Skill show command", "Skill cannot drift from the CLI". Owned paths: `internal/skill/` (new, with `clavis/` holding placeholder files that slice 2 replaces), `internal/cli/skill_commands.go`, `internal/cli/skill_test.go`, `internal/cli/skill_drift_test.go`, `internal/cli/cli.go` (command list), `internal/cli/result.go` (the install result rendering). Verify: `go test -race ./internal/cli/ ./internal/skill/ && make build-cli`.

- [x] 1.1 Embed the skill directory with the version marker rendering; implement `skill install` (target resolution for user, project, `--agent` and `--dir`; marker detection; written, updated, unchanged, refused outcomes; `--force`; `--dry-run`; temporary-file writes) and `skill show`; JSON and text results; exit codes. Tests in a temporary home for every scenario in the spec, plus permission and I/O failure paths and the hint texts.
- [x] 1.2 Implement the drift guard: invocation and flag extraction against the command tree, hint comparison, entry and reference line bounds; prove it fails on a planted bad flag.
- [x] 1.3 Independent acceptance review of slice 1; record the revision. Reviewed 2026-09-14 at `75c7773`: ACCEPT WITH FOLLOW-UPS, all applied by the coordinator in the follow-up commit: the drift guard now lets a value-taking flag own the next token (so `--start -1h` is a value) and ends a command line at an unquoted `#`; only the entry must open with frontmatter; the no-session assertion uses the real user configuration directory; the design's marker position and exit-code sentences match the code. Accepted deviations: the root command literal moved into `newRootCommand` so the guard walks the tree the binary runs; a forced replacement reports `written`; a directory without `SKILL.md` counts as foreign; a refusal renders as JSON because `INVALID_ARGUMENT` forces it. The service hints the skill quotes are listed by hand in the guard and asserted verbatim from the database package, which owns the constants. Handwritten +1,584/−37, no generated output.

## 2. Skill content, smoke, docs and rehearsal

Requirements: agent-skill "Embedded skill content"; project-bootstrap smoke and quality requirements. Depends on slice 1. Owned paths: `internal/skill/clavis/*.md`, `scripts/smoke.mjs`, `README.md`, `docs/PRD.md` (7.6 and 12), `reports/` (ignored). Verify: `go test -race ./internal/cli/ ./internal/skill/ && make check && make smoke`.

- [x] 2.1 Write `SKILL.md` and the three references per design decision 4, from README, the specs and the real-data notes, with placeholder connection names and no real host; the drift guard passes; extend smoke with the install, show, reinstall, refusal and force cases in a temporary home.
- [x] 2.2 README "Agents" section and the agent setup in Quick start; PRD 7.6 marked implemented with the install route, 12 updated.
- [x] 2.3 Rehearsal: a fresh agent session with only the installed skill against the smoke stack completes one task per provider (row counts per status over a day; error rate by job over an hour; warnings per service over fifteen minutes with JSON messages unpacked) and states its limits without coaching; record the verdict and any prose fix it prompted. Run 2026-09-14 at `7a1a91e` against a seeded stack (a payments table of 1,440 rows over a day with a read-only role, request counters for two jobs over an hour, 180 JSON log lines for two services over fifteen minutes) with the skill installed through `skill install --dir` into an isolated directory and a fresh agent session told only where the skill and the CLI were: all three answers were correct (84 failed payments totalling 21,072.00 among 1,439; error rates 12.50% for `api` and 41.67% for `web`, cross-checked against raw counter differences; `checkout` with 15 warnings, all "slow upstream response"), each with its commands, time range and completeness stated, discovery run first on every source and every result checked for truncation. Its feedback prompted eight prose fixes, applied before review: discovery sees stored fields only and a severity is often inside the JSON message; the `filter` pipe with `:=` for an exact value; the level values are whatever the application writes; `--limit` applies to the rows a query returns after its pipes; an error-rate recipe with `increase` and the raw-difference cross-check; how to ask each source for its clock to pin absolute times; the `connections check --connection <ref>` syntax; shell quoting of SQL literals; `--end` exclusive moved to the time formats. It also guessed that `5xx` meant the only `500` series present and that the two services were the payments services, both stated as assumptions.
- [x] 2.4 Independent acceptance review of slice 2 and the whole change against the PRD's SKILL-01 to SKILL-04; record the revision, limits and any exceptions for owner acceptance before archive. Reviewed 2026-09-14 at `537b5e5`: ACCEPT WITH FOLLOW-UPS, none blocking, all applied by the coordinator: the failure table no longer sends a member to `connections check` (an administrator's command) but to `connections get` and its last check; exit 2 and the provider mismatch are described as the server's refusal before any source is contacted; the envelope names `schemaVersion` and `ok` and text output is called a rendering for people; `clavis query --help` is in the setup block; the LogsQL word-filter parenthetical is corrected; a metrics error envelope saying `timeout` is listed under `SOURCE_TIMEOUT`; `--filter` names its four flags. The PRD 7.6 point on administrative requests, which the proposal had scoped out, is now met by an "Administration" section in the entry (the verb vocabulary, `--dry-run` first, `FORBIDDEN` means ask an administrator, a grant is never created because data suggested it) rather than by narrowing the PRD. Coverage table confirmed SKILL-01 to SKILL-04 and the eight teaching points of PRD 7.6. Verified: every command and flag exists in the tree the binary runs, every bound and failure code the skill states matches the specs and the code, smoke covers install, refusal, force, dry run and show in an isolated home. Whole change +2,331/−40 handwritten before these follow-ups, no generated output.
- [ ] 2.5 Run clean-checkout `make setup`, `make check`, `make smoke` and `make test-mutation-full`; report handwritten and generated line counts separately; confirm no tracked source is rewritten by successful checks.
