Slices are ordered by dependency. Each slice names its requirements, owned paths and verification commands; its implementation task and its acceptance-review task are recorded separately, and the reviewer never approves their own implementation. Slice 1 lands first; slices 2 and 3 depend on it; slices 4 and 5 depend on slice 2's contract but can proceed against fakes in parallel with slice 3; slice 6 needs every prior slice. Expect roughly 350–500 changed handwritten lines across the whole change; individual slices stay within the rescoping signal, and generated sqlc/templ output is reported separately from handwritten size.

## 1. Contracts, migration and generated queries

Requirements: user-administration "Administrator-only user operations", "Bounded user listing"; local-authentication "Secret-free authentication events" (allowlist and migration scenario); platform-initialization inherited migration/sqlc rules. Owned paths: `internal/auth/contracts.go`, `internal/auth/errors.go`, `internal/auth/events.go`, `internal/database/migrations/002_user_administration.sql`, `internal/database/queries/users.sql`, `internal/database/sqlc/`, related `_test.go`. Verify: `make generate-db && make check-db-generated && make check-sql-boundaries && go test ./internal/auth/ ./internal/database/ -run 'Migrat|Manifest|Generated|Event'`.

- [x] 1.1 Add the `Administration` interface, `UserRecord`/`UserList`/`CreateUserRequest`/`ResetPasswordRequest`/`SetRoleRequest`/`UserMutation` DTOs, administration path constants, `USERNAME_TAKEN` and `LAST_ADMINISTRATOR` failures and the extended event action/outcome allowlist; verify `LookupFailure` tables, `Event.Valid` and contract tests cover the new values and that no DTO with a `Secret` field is a result type.
- [x] 1.2 Add forward migration `002` replacing the `auth_events` action/outcome check constraints, add named queries for insert/list/set-disabled/set-role/set-password-hash/count-enabled-administrators, regenerate sqlc output; verify a fresh database applies `001`+`002` once under the shared lock with correct checksums, existing rows are unchanged, new actions insert and old ones still insert, generation checks pass without rewriting sources, and the CLI-only build stays Go-only.
- [x] 1.3 Independent acceptance review of slice 1 against the inherited persistence, allowlist and immutability requirements; record the reviewed revision, generated-output size and any findings before slices 2–3 start. Reviewed 2026-09-11 on the working tree over `7b88028`: ACCEPT, no blocking findings; handwritten +342/−22 (132 non-test), generated 211 lines; `EXPLAIN` confirmed the unique username index serves ordered listing, so the optional index stays omitted. Non-blocking: design decision 2 now names `UsernameExists`; listing order follows database collation.

## 2. Administration service

Requirements: user-administration "Administrator-only user operations", "Blocking and password reset revoke sessions", "Explicit administrator role assignment with a lockout guard", "Bounded user listing"; local-authentication "Secret-free authentication events" (user-administration scenario, audit storage fails). Depends on slice 1. Owned paths: `internal/database/admin.go`, `internal/database/admin_test.go`, `internal/database/events.go`. Verify: `CLAVIS_BACKEND_TEST_DATABASE_URL=... go test -race ./internal/database/`.

- [x] 2.1 Implement `ListUsers`, `CreateUser`, `SetUserDisabled`, `ResetPassword` and `SetRole` on `LocalAuth` with the design's transaction shape: readiness gate, advisory administration key, ordered row locks, session recheck, target existence, last-administrator count, generated-query mutations, session revocation for block/reset, event write and commit; hash before `Begin`; verify each denial outcome (`unauthenticated`, `forbidden`, `user_not_found`, `username_taken`, `last_administrator`) commits exactly one event and no mutation.
- [x] 2.2 Cover behavior with real-PostgreSQL tests: created member signs in on both transports; block denies existing sessions and later login, unblock restores login but not sessions; reset invalidates the old password and sessions and honors the hashing budget; promote authorizes an existing session, demote forbids it; self-targeting with another admin present; concurrent mutual demotion leaves exactly one admin; listing order, fields, 1,000 bound and `truncated`; injected audit-write failure returns unavailability without mutation; sentinel passwords never appear in events, errors or logs.
- [x] 2.3 Independent acceptance review of slice 2 against the delta specs and inherited locking/event/deadline decisions, including a race and held-lock timeout check; record the revision and verification limits. Reviewed 2026-09-11 on the working tree over `8213ec8` (implementer and reviewer both Fable 5.1): ACCEPT twice, first for the flat-admin version, then for the owner-decided GitLab owner model with `SELF_TARGET`. `admin.go` 354 lines, `admin_test.go` 657 lines. Findings: `LAST_ADMINISTRATOR` is unreachable through the API once self-targeting is refused and is kept as a directly tested safety net; concurrent mutual demote/block losers receive `FORBIDDEN`/`UNAUTHENTICATED`; a transient Docker clock skew once failed a wall-clock assertion in slice 1's query test and did not reproduce.

## 3. JSON administration routes

Requirements: user-administration "JSON administration transport"; local-authentication "Explicit authentication transports" and adapter-rejection event scenarios (inherited). Depends on slice 2 (or a fake `Administration` for tests). Owned paths: `internal/server/admin.go`, `internal/server/auth.go` (route mounting, `auditRejection` route mapping, `decodeJSON`), `internal/server/server.go` (dependency wiring), `internal/server/*_test.go`. Verify: `go test -race ./internal/server/`.

- [ ] 3.1 Mount the six routes through the operation middleware with bearer-only sessions, origin checks, 8 KiB strict JSON decoding with unknown-field/trailing-data rejection, empty-body checks, `serviceOwnsEvent`, 201 for create and `no-store`; extend `auditRejection` to map administration routes to `user.*`/`users.list` actions by route pattern; make `HandlerWithAuth` require the administration dependency.
- [ ] 3.2 Add contract tests: each route's success body, every documented error status/code, cookie-only requests receive 401 JSON, browser/partial headers never redirect, oversized/form-encoded/unknown-field bodies fail without hashing or reflection, pre-service rejections record one event and service outcomes are not double-recorded, held-lock timeout returns 503 within the deadline.
- [ ] 3.3 Independent acceptance review of slice 3 against the frozen transport rules and the design's route table; record the revision.

## 4. Browser user list

Requirements: user-administration "Read-only administrator user list in the browser"; local-authentication "Protected browser authentication" (admin page list, outage scenario); embedded-web reproducible generated assets (inherited). Depends on slice 1 DTOs; can use a fake `Administration`. Owned paths: `internal/web/auth.templ`, `internal/web/auth_templ.go`, `internal/web/auth_models.go`, `internal/web/*_test.go`, `internal/server/auth.go` (`adminBrowser` only). Verify: `make generate-web && make check-web-generated && go test ./internal/web/ ./internal/server/ -run 'Admin|Render'`.

- [ ] 4.1 Extend `AdminModel` with users and truncation, render the table with escaped username, role, enabled/blocked state and UTC creation time plus a truncation notice, keep sign-out and appearance controls, add no forms or scripts; call `ListUsers` from `adminBrowser` after the role check and map failures to the existing safe 503 document; regenerate and commit templ Go.
- [ ] 4.2 Add rendering and HTTP tests for populated, empty and truncated lists, escaped markup-like text within the username character set, member 403 unchanged, list failure 503, and no success event for the page load; verify generation checks do not rewrite tracked sources.
- [ ] 4.3 Independent acceptance review of slice 4 against the browser requirements and the "no management forms" boundary; record the revision.

## 5. CLI user commands

Requirements: cli-authentication "Administrative user commands" plus inherited "Safe local CLI sign-in", "Bounded and non-disclosing authentication transport" and "Authentication preserves CLI output conventions". Depends on slice 1 contracts; tests use the fake HTTP server. Owned paths: `internal/cli/auth_commands.go`, `internal/cli/auth_transport.go`, `internal/cli/result.go`, `internal/cli/*_test.go`. Verify: `go test -race ./internal/cli/ && make build-cli`.

- [x] 5.1 Register the `users` group (`list`, `create`, `block`, `unblock`, `reset-password`, `set-role`) sharing server/timeout flags and `runAuth`; validate UUID/role/username and read passwords through the login input path before any I/O; require a cached session; call only the documented endpoints; accept 201 and the new error codes in the transport allowlist; add text rendering for user records, lists and mutations.
- [x] 5.2 Add unit and subprocess tests: automation create with `--password-stdin`, interactive reset with hidden prompt separate from stdout, member `FORBIDDEN`, `LAST_ADMINISTRATOR` pass-through, undocumented responses map to `INVALID_RESPONSE`, exit 2 for malformed UUID/role/positional/noninteractive input without a request, no password or token in JSON/text/errors, offline help and Go-only build unaffected.
- [x] 5.3 Independent acceptance review of slice 5 against the CLI conventions; record the revision. Reviewed 2026-09-11 on the working tree over `2a5b19d`: ACCEPT, no blocking findings; handwritten +453/−54 plus 506 lines in two new files; `go list -deps` confirms the CLI stays free of web/database packages. Coordinator integration edit: `auth.MaxListingBody` (256 KiB) for `GET UsersPath` only, with `TestUsersListingBodyLimit`. Notes for slice 3: the server must not return 429 on list/block/unblock/role; the `RATE_LIMITED` message still says "sign-in attempts".

## 6. Integration, smoke, documentation and whole-change verification

Requirements: all delta specs end to end; project-bootstrap "Repeatable quality and smoke checks" and "Focused mutation testing" (inherited). Depends on slices 1–5. Owned paths: `scripts/smoke.mjs`, `README.md`, `docs/PRD.md` (implementation-status sentence only), `.gremlins.yaml` if targets change, `reports/` (ignored). Verify: `make check && make smoke && make test-mutation`.

- [ ] 6.1 Replace member/disable/demote/restore fixture SQL in smoke with CLI `users` commands against the copied binary; cover member sign-in on both transports, block/unblock, reset with old-password failure, promotion enabling revoke, `LAST_ADMINISTRATOR` when the bootstrap admin is alone, demotion after promotion, and per-action event counts; keep the expiry fixture; confirm cleanup removes only owned resources including new CLI homes.
- [ ] 6.2 Update the README command table and CLI section for `users` commands and password input, note the 1,000-user listing bound, the last-administrator guard and that self-service password change and account recovery are not part of the MVP (administrators own every password); update the PRD implementation-status sentence in section 12; verify no credentials or deployment manifests are added.
- [ ] 6.3 Run clean-checkout `make setup`, `make check`, real-database race tests, `make smoke` and `make test-mutation`; report handwritten and generated line counts separately; confirm no tracked source is rewritten by successful checks.
- [ ] 6.4 Whole-change independent review against the inherited requirements, all three delta specs and the design's inherited-decisions table; record the final revision, verification limits and any accepted exceptions for owner acceptance before archive.
