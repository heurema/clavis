Three slices in dependency order; implementation and independent acceptance review are separate tasks, and a reviewer never approves their own implementation. Each slice leaves a working interface. Expect roughly 900 to 1,200 changed handwritten lines across about 30 files (a whole-interface replacement; flagged in the proposal), plus regenerated `*_templ.go` files and the ignored assets, reported separately. Run every Go suite under `caffeinate` when it includes the database-backed packages.

## 1. Remove the status page, the readiness fragment and htmx

Requirements: embedded-web "One origin for the interface and API" (root redirect, unknown `/ui/readiness`), "Distinct HTML and JSON representations", removed "Interactive controls survive partial updates"; project-bootstrap removed "Web foundation displays actual readiness"; platform-initialization "Initialization does not withhold public diagnostics"; local-authentication "Public documents receive stale cookies during an outage" (`/` redirects without database access). Owned paths: `internal/server/server.go`, `internal/server/web_test.go`, `internal/server/contracts_test.go`, `internal/web/page.templ` (delete), `internal/web/page_templ.go` (delete), `internal/web/readiness.go` (delete), `internal/web/render_test.go`, `internal/web/auth_view_test.go` (readiness fragment tests), `internal/web/assets.go`, `internal/web/assets_test.go`, `internal/web/ui/icon/icon.templ` (orphaned icons), `internal/web/testdata/composition.templ` (htmx attribute), `web/scripts/readiness.js` (delete), `web/styles/app.css` (readiness classes), `web/package.json`, `web/pnpm-lock.yaml`, `scripts/build-web-assets.mjs`, `scripts/build-web-assets.test.mjs`, `scripts/build.test.mjs`, `scripts/smoke.mjs`, `README.md`, `docs/PRD.md`. Verify: `make generate-web && make check` (includes the dead-code check, the node script tests and the copied-executable build test) and `make smoke`.

- [x] 1.1 Replace `GET /` with a `303` to `/admin/users` carrying `Cache-Control: no-store` and no database access; remove `GET /ui/readiness`, `web.Page`, `web.Readiness`, `readinessView` and the readiness templates; update the server tests (root redirect without a ping, unknown paths including `/ui/readiness` are 404, the deadline test covers `/health/ready` only, presentation headers on JSON routes without the htmx headers).
- [x] 1.2 Drop htmx and the readiness script from the asset pipeline: the embed list, the build script and its notices, the package manifest and lockfile, the asset tests, the build test's route table (`/` answers `303` to `/admin/users`; three assets), and the smoke script's public routes; remove the readiness CSS classes; remove icons orphaned by the page (keep any the composition fixture uses) so the dead-code check passes.
- [x] 1.3 Update README (quick start opens sign-in, the web paragraph without the readiness widget, the stack line) and PRD 7.8 and 12 for the removed status page and htmx.
- [x] 1.4 Independent acceptance review of slice 1 against the requirements above; record the revision and the verification commands run. Reviewed 2026-09-14 on the working tree over `6f5185a`: ACCEPT WITH FOLLOW-UPS, no blocking findings; the coordinator applied the three recommended follow-ups before committing: the sign-in document's "Check environment setup" link to the removed page is deleted, the unused `X-Clavis-Fragment` handling is removed from `render.go` and its tests, and the asset boundary test probes the two removed assets as 404. Accepted deviations: the render-failure test now goes through `/login` and no longer asserts a `WEB_RESPONSE_FAILED` log line (no remaining route logs it); the build test keeps a `/ui/readiness` row asserting 404; the smoke script asserts only the first hop of the root redirect until slice 3 mounts `/admin/users`; `auth.LoginOutcome` still lands on `/admin` until slice 2. Deferred to slice 2: the stale delegation comment in `appearance.js` and the README sentence about the switch. Verification: `make check` passed (`reports/slice1-check.log`, lint clean, no unreachable code, node script tests including the copied-executable build test, `go test -p 1 ./...` without the database variable); after the follow-ups `make generate-web`, `make check-web-generated`, `make check-dead-code` and `go test ./internal/web/ ./internal/server/` passed again. `make smoke` passed on the committed slice (`1c807e0`, `reports/slice1-smoke.log`). Handwritten +115/−585 across 26 files (tests included); generated `*_templ.go` +10/−652; lockfile −9.

## 2. Public documents: sign-in, auth error, top bar and appearance button

Requirements: local-authentication "Protected browser authentication" (sign-in document, `/admin/users` landing, safe rendering); project-bootstrap "Usable visual foundation" (appearance button with `aria-pressed`, keyboard operation, persisted preference); embedded-web "Browser assets work without third-party network access". Depends on slice 1. Owned paths: `internal/auth/browser_contract.go` (`LoginOutcome` location), `internal/auth/*_test.go` for that location, `internal/web/auth.templ` (`publicDocument`, `Login`, `AuthError`, the appearance button), `internal/web/auth_view_test.go`, `internal/web/components_test.go`, `internal/web/testdata/composition.templ`, `internal/web/ui/icon/icon.templ` (`Sun`, `Moon`), `internal/web/ui/switch` (delete), `web/scripts/appearance.js`, `web/styles/app.css`, `internal/server/auth_test.go` (post-login location). Verify: `make generate-web && make check`.

- [x] 2.1 Build `publicDocument` with the slim top bar (mark, Clavis, appearance button) and rewrite `Login` as the 384-pixel column with heading "Sign in", the alert (message, rate-limit hint, cleared-password sentence) and the two-field card; keep the input attributes and drop the helper text; rebuild `AuthError` on the same document; make `LoginOutcome` land on `/admin/users` and update its tests.
- [x] 2.2 Replace the switch with the appearance button (both icons rendered, one shown per appearance, `aria-pressed` maintained) and update `appearance.js` to click delegation without the htmx listener; delete the `switch` component; update the composition fixture and the view tests (button present with `aria-pressed`, no `hx-` attributes, no helper text, escaping and sentinel assertions unchanged); keep the appearance preference persisted.
- [x] 2.3 Independent acceptance review of slice 2, including a keyboard pass through sign-in and the appearance button in a browser; record the revision. Reviewed 2026-09-14 on the working tree over `1c807e0`: ACCEPT WITH FOLLOW-UPS, no blocking findings. The reviewer served the documents with the database port closed and inspected the DOM and screenshots: `/login` 200 with the CSP, `/` 303 to `/admin/users`, `/admin` 303 to `/login`, the alert, escaping and password-never-echoed guarantees intact, the moon shown in light and the sun in dark with `aria-pressed` maintained, and the controls keyboard-operable by structure (real buttons, labelled inputs, focus-visible outlines). Coordinator applied two test follow-ups before committing: `renderAuth` asserts exactly one attribution link per document and the sign-in heading assertion no longer couples to its classes. Carried into slice 3: derive the login redirect header from `auth.LoginOutcome` instead of a second literal in the handler. Accepted deviations: the login handler literal and the database test moved to `/admin/users`; `templui.go` keeps only `TwMerge`; the build test's CSS marker changed with the switch; `AuthError` keeps its heading; headless Chrome clamps at 500 px so the 400 px layout was proven in-page. Verification: `make check` passed (`reports/slice2-check.log`); `go test ./internal/web/` again after the follow-ups. Handwritten +146/−223 across 16 files; generated `*_templ.go` +454/−651; no lockfile change.

## 3. Administration shell and the three list pages

Requirements: local-authentication "Protected browser authentication" (three routes, `/admin` redirect, shell contents, fail closed on any list, member 403); user-administration "Read-only administrator user list in the browser"; connection-management "Read-only connections table in the browser"; connection-grants "Read-only grants table in the browser"; project-bootstrap "Usable visual foundation" (current page announced, status without colour). Depends on slice 2. Owned paths: `internal/server/auth.go` (routes, loader, timeout fallback), `internal/server/admin_page_test.go`, `internal/server/connections_page_test.go`, `internal/server/grants_page_test.go`, `internal/server/auth_test.go`, `internal/server/contracts_test.go`, `internal/web/auth.templ` (`adminShell`, the three pages, the status helper), `internal/web/auth_models.go` (`AdminPage`, counts, status kinds), `internal/web/auth_view_test.go`, `internal/web/ui/icon/icon.templ` (`Users`, `Link`, `ShieldCheck`, `LogOut`), `internal/web/ui/badge/badge.templ` (unused variants), `web/styles/app.css` (status tokens), `README.md`, `docs/PRD.md`. Verify: `make generate-web && make check`, `CLAVIS_BACKEND_TEST_DATABASE_URL=... go test -race -p 1 ./internal/server/`, and `make smoke`.

- [x] 3.1 Mount `GET /admin/users`, `/admin/connections` and `/admin/grants` on the shared loader with the page value, redirect `GET /admin` to `/admin/users`, extend the timeout fallback to the `/admin/` prefix; update the server page tests to their own routes and add assertions for the counts, `aria-current`, the `+` suffix on truncation, and the 503 when any of the three lists fails on any page; update the path lists in `auth_test.go` and `contracts_test.go`.
- [x] 3.2 Build `adminShell` (sidebar with mark, appearance button, the three entries with icons and counts, the current page marked, the user, role and sign-out form at the bottom; a top row below `lg`) and the three pages with the approved table styling, the `status` helper with the three tokens, plain-text role and provider, label chips and UTC times; keep every escaping and sentinel-target guarantee; remove badge variants and helpers orphaned by the new tables so the dead-code check passes; view tests for the shell, each page, each status kind and the truncation notice.
- [x] 3.3 Update README (the three pages, the shell) and PRD 7.8 and 12 for the shell; update the smoke script to sign in and fetch the three pages.
- [x] 3.4 Independent acceptance review of slice 3, including a browser pass over the three pages in light and dark appearance at 1440 and 400 pixels; record the revision. Reviewed 2026-09-14 on the working tree over `065a80e`: ACCEPT WITH FOLLOW-UPS; one blocking test finding, applied by the coordinator before committing: a database-backed test still requested `/admin` (now a public redirect) and expected the member 403, so it requests `/admin/users`. Also applied: the sidebar shows the same role label as the table through one `roleLabel` helper, and a stale test name lost its ordering suffix. Accepted deviations: the sidebar username keeps `break-all` (a 232-pixel column with 64-character unbreakable names); status text assertions match bare words, backed by unit tests of the status helpers; `text-status-bad` colours the word as well as the dot for problems; screenshots were rendered from the production templates with sample data and served assets statically; no `/admin` row in the copied-executable build test (the redirect is proven by the contract test and smoke). The reviewer confirmed the loader order and first-error semantics unchanged, no target, description, scope or identifier in display data, every class literal present in the built stylesheet, the tokens defined for both appearances, exactly one form and one attribution link per page, `aria-current` once, no dropped test guarantee, and the smoke additions sound. Screenshots at 1440 light and dark and at the 500-pixel headless minimum show no overflow, the sidebar as a top row and the table scrolling in its card. Verification: `make check` passed (`reports/slice3-check.log`); `make smoke` passed with the new browser sign-in, three pages and sign-out step (`reports/slice3-smoke.log`); `go test -race -p 1 ./internal/server/` against an isolated `clavis_backend_test` database passed after the test fix (`reports/slice3-dbtest.log`); the web and server unit suites, formatting, generated-source and dead-code checks passed again after the follow-ups. Handwritten +790/−262 across 17 files; generated `*_templ.go` +1153/−588.

## 4. Whole-change verification

Requirements: all seven delta specs end to end; embedded-web "Reproducible generated assets" and "Self-contained server distribution"; project-bootstrap quality requirements. Depends on slices 1 to 3. Owned paths: `reports/` (ignored) and any coordinator integration edits under the slice rules. Verify: clean-checkout `make setup && make check`, the real-database race suite, `make smoke`, `make test-mutation-full` (detached, under `caffeinate`).

- [x] 4.1 Run clean-checkout `make setup`, `make check`, the real-database race suite for auth, database and server, `make smoke` and `make test-mutation-full`; report handwritten and generated line counts separately; confirm no tracked source is rewritten by successful checks and that the copied executable serves only the three assets. Evidence 2026-09-14 at `7ccd52e` plus the follow-up `9fa50b3`: a clean clone of the branch ran `make setup` and `make check` to "All checks passed" (`reports/final-clean-check.log`; the only drift afterwards is pnpm adding its own `@pnpm/exe` self-management entry to the lockfile, pre-existing and unrelated); `go test -race -p 1` for auth, database and server passed against an isolated `clavis_backend_test` database on the published port 5432 (`reports/final-dbtest.log`; the `.env` database port does not match the running container, noted for the owner, nothing changed); `make smoke` passed including the browser sign-in, the three administration pages and sign-out (`reports/final-smoke.log`); the copied-executable build test proves `/` redirects, `/login` renders, exactly the three assets are served and `/ui/readiness` is 404. `make test-mutation-full` with the default eight workers: 980 killed, 114 lived, 617 not covered, 2 timed out (previous change 863 / 110 / 566 / 2), source unchanged (`reports/final-mutation.log`, `reports/mutation-summary.json`). Lines since `main`: handwritten +1,012/−1,018 across 36 files (tests, scripts and docs included), generated `*_templ.go` and lockfile +1,142/−1,437 across 6 files, OpenSpec artefacts +358. No tracked source was rewritten by any successful check.
- [x] 4.2 Whole-change independent review against the inherited requirements, the seven delta specs and the design's inherited-decisions table, with a browser pass over sign-in, the error document and the three pages; record the final revision, limits and any exceptions for owner acceptance before archive. Reviewed 2026-09-14 at `7ccd52e`: ACCEPT WITH FOLLOW-UPS, no blocking findings. Coverage table confirmed every delta requirement and scenario has a unit, contract, copied-executable or smoke proof; the removed status page, fragment route, readiness script, htmx, switch and partial-update handling leave no trace in source; every retained inherited requirement holds (one origin, public documents and assets without database access, credential-only GETs, no token in HTML or logs, framing rejected, escaping, no target or secret in display data, bounded lists with notices, UTC times, persisted appearance, explicit embed list with attribution, no CDN or font service, dead-code check); both departures are recorded in proposal, design, PRD 12 and README with reason and migration in the delta specs; artefacts, README and PRD agree on routes, shell and status convention; no security regression in the diff; every deleted assertion belongs to a removed feature or was replaced on the new routes. Follow-up applied in `9fa50b3`: the sidebar initial takes the first rune. Accepted as is: failed check outcomes render their raw identifier while `reachable` is capitalised; the sign-in page's home link bounces through the root redirect; the screenshots predate the role-label follow-up. Exceptions for owner acceptance, proven by structure or screenshot only: keyboard operation of the shell (real buttons and links with accessible names and focus outlines; manual pass in the slice 2 review); the persisted appearance preference (unchanged script, no browser-driven test); assets without third-party network access (no external URL in the sources, references resolved against the embed); the layouts at 1440 and 400 pixels in both appearances (headless minimum 500 pixels); the root redirect chain proven as separate hops in smoke. The maintained specs under `openspec/specs/` still carry the status-page wording until this change is archived.
