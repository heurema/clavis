Four slices in dependency order; implementation and independent acceptance review are separate tasks, and a reviewer never approves their own implementation. Slice 1 changes Go code and the Makefile compile recipe and leaves a green tree; slice 2 (image, compose smoke) and slice 3 (chart, lint tooling) both depend on slice 1 and can run in parallel with non-overlapping ownership; slice 4 needs everything. Estimates: handwritten production code 150 to 220 Go lines, Makefile and Dockerfile 120 to 180 lines, chart templates, schema and value sets 500 to 700 lines of YAML and JSON, scripts 400 to 600 lines, tests 400 to 550 lines, documentation 150 to 250 lines. The chart and the scripts each exceed the 400-line rescoping signal on their own; they are kept as slices because splitting a chart's templates from its schema, or a verification script from its assertions, would leave no independently reviewable intermediate state. Run every Go suite that includes the database-backed packages under `caffeinate`, against the test database on port 5432. Docker is required for slices 2 and 4; a laptop with about 6 GB free for kind.

## 1. Readers, health endpoints, build identity and the compile recipe

Requirements: project-bootstrap "Independent liveness and readiness" (delta), "Required encryption key configuration" (delta), platform-initialization "Safe bounded bootstrap secret handling" (delta), connection-management "Encrypted credentials at rest" (delta), embedded-web "Distinct HTML and JSON representations" (delta), deployment-packaging "Build identity". Owned paths: `internal/secrets/keyfile.go`, `internal/secrets/protected.go` (new), `internal/secrets/secrets_test.go`, `internal/secrets/fifo_test.go`, `internal/database/initialize.go`, `internal/database/initialize_test.go`, `cmd/server/main.go`, `cmd/server/main_test.go`, `internal/server/server.go` (health routes, `server_started`), `internal/server/server_test.go`, `web_test.go`, `contracts_test.go`, `auth_test.go`, `database_test.go`, `internal/cli/doctor.go`, `internal/cli/cli_test.go`, `internal/cli/auth_test.go`, `scripts/smoke.mjs`, `scripts/build.test.mjs`, `Makefile` (`compile-server`, `build-server`, `VERSION`/`COMMIT`/`DATE`), `README.md` (health paths, Commands rows for the split targets), `docs/PRD.md` sections 7.8 and 12 (health paths). Verify: `go test -race ./internal/secrets/ ./internal/database/ -run 'Key|Bootstrap|Protected|Password'`, `go test -race ./internal/server/ ./internal/cli/ ./cmd/...`, `make build-server && make smoke`, `make compile-server VERSION=1.2.3 COMMIT=abc DATE=2026-09-15 && ./bin/server` with a valid environment shows the stamped values in `server_started` and `curl /healthz` reports version `1.2.3`, `node --test scripts/build.test.mjs`, `grep -rn '/health/' cmd internal scripts README.md docs` returns nothing.

- [ ] 1.1 Add `secrets.ProtectedFile(os.FileInfo) bool` implementing the rule from design decision 2 (reject any of `0o037`, accept owner-only, accept group-read only when the file's gid is the effective gid or in `os.Getgroups()`), and use it in `LoadKeyFile` and `ReadBootstrapPassword` on the opened descriptor's `Stat`; keep error categories, symlink handling, nonblocking open and size bounds unchanged. Tests for both readers: `0400`, `0600`, `0440`, `0640` with the primary group accepted; `0440` with a foreign group refused (choose a group from `os.Getgroups()` the file can be chowned to, skip with a message when none exists); `0460`, `0444`, `0404`, `0604`, `0620`, `0410` refused; FIFO, directory and oversize still refused; a symlink to an accepted `0440` file accepted.
- [ ] 1.2 Rename the health routes to `/livez` and `/readyz` with unchanged bodies and codes, add `/healthz` per design decision 3 (`status` `ok` or `unhealthy`, `version`, nested `checks.live` and `checks.ready`, 200 only when ready, same timeout, `no-store`), remove the `/health/*` paths, point `clavis doctor` at `/readyz`, and rename every test, `smoke.mjs` and `build.test.mjs` reference; add server tests for `/healthz` in the ready, dependency-unavailable and initialization-pending states and for browser headers, a not-found test for each old path, and one `/healthz` assertion in `smoke.mjs` in the ready and the outage states; update the README health sentences and PRD sections 7.8 and 12.
- [ ] 1.3 Add `version`, `commit` and `date` from `buildinfo.Current()` to the `server_started` log entry and `buildinfo.Version` to `/healthz`; assert the log fields in the server test through the captured logger and the version in the `/healthz` test; confirm the `/livez` and `/readyz` bodies are unchanged.
- [ ] 1.4 Split `build-server` into `compile-server` (atomic temp-dir output, `CGO_ENABLED=0 go build -trimpath -ldflags` with `-X` for the three `buildinfo` variables from Make variables `VERSION ?= dev`, `COMMIT ?= unknown`, `DATE ?= unknown`, honouring `GOOS`/`GOARCH` from the environment) and `build-server` (gates, `build-web-assets`, then `compile-server`); update `.PHONY`, the README Commands table and `scripts/build.test.mjs` so it covers `compile-server` producing a binary without running the gates and `build-server` still running them.
- [ ] 1.5 Independent acceptance review of slice 1 against the five delta specs and "Build identity"; confirm no production path other than the two readers, the health routes, `doctor` and the startup log changed; confirm no `/health/` reference remains outside archived changes; record the revision and line counts.

## 2. Container image and compose image smoke

Requirements: deployment-packaging "Server container image", "Image and chart verification commands" (image smoke scenario); project-bootstrap "Repeatable quality and smoke checks" (delta, image smoke cleanup); embedded-web "Reproducible generated assets" (inherited, must hold for the image build). Depends on slice 1. Owned paths: `Dockerfile` (new), `.dockerignore` (new), `Makefile` (`image`, `smoke-image`, `IMAGE` variable), `compose.yaml` (`app` service under `profiles: [app]`), `scripts/smoke-image.mjs` (new), `scripts/smoke-image.test.mjs` (new, argument and summary handling only), `README.md` Commands rows and prerequisites. Verify: `make image && docker run --rm clavis:local` fails fast with `CLAVIS_DATABASE_URL REQUIRED` as a non-root user; `make smoke-image` passes against `make dev-db`; `docker buildx build --platform linux/amd64,linux/arm64` succeeds (no push); `docker build` of a checkout with a deliberately stale `_templ.go` fails at `check-web-generated`.

- [ ] 2.1 Write the Dockerfile from design decision 1: builder on `$BUILDPLATFORM` from a digest-pinned `golang:1.27.1-bookworm`, Node 26.8.2 and pnpm 12.3.4 installed from pinned archives with checksums, dependency layers before source, `make install-templ install-sqlc check-db-generated check-web-generated build-web-assets`, then `GOOS=$TARGETOS GOARCH=$TARGETARCH make compile-server VERSION COMMIT DATE` from build args; runtime from digest-pinned `gcr.io/distroless/static-debian12:nonroot` with `USER 65532:65532`, `ENV CLAVIS_HTTP_ADDR=0.0.0.0:8080`, `EXPOSE 8080`, exec-form `ENTRYPOINT`, OCI labels for source, revision and version. Write `.dockerignore` per the design. Add `make image` (`docker build` with `VERSION`, `COMMIT`, `DATE` from Git when available, tag `clavis:local` by default, `IMAGE` overridable).
- [ ] 2.2 Add the compose `app` service behind `profiles: [app]` (image `clavis:local`, `depends_on` the healthy `db`, database URL to `db:5432`, `CLAVIS_PUBLIC_URL=https://clavis.local`, key and bootstrap password bind-mounted read-only from `${CLAVIS_APP_SECRETS_DIR}`, port on `127.0.0.1:${CLAVIS_APP_PORT:-8081}`, `read_only: true`, `user: 65532:65532`, `group_add` for the secrets group). Confirm `make dev-db` and `make smoke` ignore the profile.
- [ ] 2.3 Write `scripts/smoke-image.mjs` and `make smoke-image` (depends on `image`): own compose project name, a temporary secrets directory with a key and a bootstrap password at mode `0440` and gid 65532, `up --wait` for `db` and `app`, `/livez` then `/readyz` until `ready` within the deadline, `/login` and one embedded asset, `docker top` shows uid 65532, `clavis doctor` and `clavis login`/`whoami` over the published port with the built CLI, summary to `reports/smoke-image-summary.json`, and `down --volumes` of its own project in `finally`. Redact the password in output like `smoke.mjs`.
- [ ] 2.4 Independent acceptance review of slice 2: run the four verifications above, check the runtime image contains only `/server`, certificates and zoneinfo (`docker run --rm --entrypoint` is unavailable on distroless, so use `docker create` and `docker export | tar t`), confirm `.dockerignore` excludes `.env`, `.tools/`, `bin/`, `reports/`, `.local/`, `internal/web/assets/`; record image sizes per architecture and the revision.

## 3. Helm chart and chart lint

Requirements: deployment-packaging "Helm chart contract", "Secrets by reference", "Bootstrap as an explicit installation input", "Documented upgrade behaviour", "Image and chart verification commands" (chart lint scenario); project-bootstrap "Repeatable quality and smoke checks" (delta, pinned tools and `chart-lint` in `check`). Depends on slice 1 (image name and port only; templates can be written against the design). Owned paths: `deploy/charts/clavis/**` (new), `Makefile` (`HELM_VERSION`, `KIND_VERSION`, `KUBECONFORM_VERSION`, `install-helm`, `install-kind`, `install-kubeconform`, `install-kube-schemas`, `chart-lint`, `setup` and `check` additions), `scripts/chart-lint.mjs` (new, iterates `ci/` and asserts the failure sets), `scripts/chart-lint.test.mjs` (new), `README.md` prerequisites, Commands rows and the Deployment section. Verify: `make setup` installs the three tools and the schema sets offline afterwards; `make chart-lint` passes; editing `ci/default.yaml` to drop `publicURL` fails naming the set; `make check` includes `chart-lint`.

- [ ] 3.1 Add the tool pins and `install-*` targets through versioned `go install` into `.tools/<tool>/bin/` (Helm 4.1.4 from `helm.sh/helm/v4/cmd/helm`, kind 0.31.0 from `sigs.k8s.io/kind`, kubeconform 0.7.0 from `github.com/yannh/kubeconform/cmd/kubeconform`) and `install-kube-schemas` downloading the pinned Kubernetes 1.34 schema set and the Gateway API `HTTPRoute` schema into `.tools/kube-schemas/` by commit; add them to `setup`; README prerequisites and pin documentation.
- [ ] 3.2 Write the chart from design decision 4: `Chart.yaml`, `values.yaml` with every value documented inline, `values.schema.json` marking `publicURL`, `secrets.database.secretName` and `secrets.encryptionKey.secretName` required and constraining `publicURL` to `https://`, `_helpers.tpl` with names, labels, selector labels, the image reference (digest over tag over `appVersion`) and the `fail` rules (public URL, both routes, bootstrap fields), Deployment (strategy, probes, security contexts, `automountServiceAccountToken: false`, env mapping for every `server.*` value and the three secrets, conditional bootstrap env and volumes, `external` handling, `extraEnv`/`extraVolumes`/`extraVolumeMounts`, scheduling values), ServiceAccount, Service, optional Ingress, HTTPRoute, NetworkPolicy, PDB, `NOTES.txt`.
- [ ] 3.3 Write the `ci/` value sets (`default`, `ingress`, `httproute`, `full`, `bootstrap`, `csi-key`, `fail-no-public-url`, `fail-both-routes`), `scripts/chart-lint.mjs` (`helm lint --strict`, then for each set `helm template` into `kubeconform -strict -summary -schema-location <.tools/kube-schemas/...>` with the Gateway API location for the HTTPRoute set; the `fail-*` sets must fail with the expected message and any other outcome fails the check; a missing schema directory fails naming `make setup`), its unit test, `make chart-lint`, and `chart-lint` appended to `check` after `build`.
- [ ] 3.4 Write `deploy/charts/clavis/README.md` with the operator contract from design decision 8 (prerequisites, three Secrets with an External Secrets Operator example, `publicURL` and TLS termination, bootstrap lifecycle, upgrade interruption and no rollback across a migration, direct PostgreSQL or session pooling only, no key rotation, backups of database and key together, CloudNativePG in one sentence, file-based providers through `external` and `extraVolumes`, `fsGroup` required when overriding the security context), and the short README Deployment section pointing at it.
- [ ] 3.5 Independent acceptance review of slice 3 against the five chart requirements: render each `ci/` set and check the Deployment field by field against "Helm chart contract" and "Secrets by reference" (probe timeouts derived from `dbCheckTimeout`, grace period from `shutdownTimeout`, `0440` secret modes, no secret value anywhere in the output), confirm no template renders a database resource, confirm the schema and the `fail` rules agree; record the revision and line counts.

## 4. Kind verification, documentation and whole-change checks

Requirements: deployment-packaging "Image and chart verification commands" (kind scenario), "Bootstrap as an explicit installation input", "Documented upgrade behaviour", "Secrets by reference" (fsGroup scenario end to end); every delta spec end to end; project-bootstrap quality and smoke requirements. Depends on slices 1 to 3. Owned paths: `scripts/verify-kind.mjs` (new), `scripts/verify-kind.test.mjs` (new, argument and manifest generation only), `Makefile` (`verify-kind`), `README.md` Commands row, `docs/PRD.md` section 12 status paragraph, `reports/` (ignored). Verify: `make verify-kind` passes on a clean checkout with Docker running; `make check`, `make smoke`, `make smoke-image`; `make test-mutation` for the Go changes of slice 1.

- [ ] 4.1 Write `scripts/verify-kind.mjs` and `make verify-kind` from design decision 7: pinned `kindest/node` digest, cluster name with a random suffix, inline PostgreSQL 18.6 Deployment and Service, image `kind-a` from the checkout and `kind-b` from a temporary copy with `internal/database/migrations/900_verify.sql` (`-- +goose Up`, one empty table), `kind load docker-image` for both, the three Secrets, `helm install` with bootstrap enabled, wait for `ready`, port-forward and `clavis doctor`/`login`/`whoami`, `helm upgrade` with bootstrap disabled, delete the bootstrap Secret, `kubectl rollout restart` and re-check, `helm upgrade` to `kind-b` and observe the old pod's readiness drop after the new pod is ready then `ready` and sign-in again, `helm template` of the installed values through kubeconform, summary to `reports/verify-kind.json`, `kind delete cluster` in `finally`; every failure names the step.
- [ ] 4.2 Update the README (prerequisites, Commands rows for `image`, `chart-lint`, `smoke-image`, `verify-kind`, the Deployment section) and the PRD section 12 status paragraph; record accepted follow-ups.
- [ ] 4.3 Run clean-checkout `make setup`, `make check` (with `chart-lint`), the real-database race suites under `caffeinate`, `make smoke`, `make smoke-image`, `make verify-kind` and `make test-mutation`; report production, chart, script, test and documentation line counts separately against the estimates; confirm no tracked source is rewritten by a successful check and that `make check` needs no network after setup.
- [ ] 4.4 Whole-change independent review against the inherited requirements, the four delta specs and the design's inherited-decisions table, with the secret-file rule, the bootstrap lifecycle and the migration upgrade verified from the kind evidence; record the final revision, limits (single replica, upgrade interruption, no rollback across a migration, no rotation) and any exceptions for owner acceptance before archive.
