Two slices plus the first release; implementation and independent acceptance review are separate tasks, and a reviewer never approves their own implementation. Slice 1 is Go only and leaves a green tree. Slice 2 is the Makefile, the workflow and the documentation, and depends on slice 1 for the version a `go install` reports. Estimates: Go production 30 to 50 lines with 70 to 110 lines of tests; Makefile 5 to 10 lines; `build.test.mjs` 20 to 40 lines; workflow 60 to 80 lines of YAML; documentation 30 to 50 lines. Neither slice exceeds the rescoping signal. No database is needed. Docker is needed to rehearse the image step.

## 1. Build identity resolution

Requirements: deployment-packaging "Build identity" (delta, the four scenarios about resolution). Owned paths: `internal/buildinfo/buildinfo.go`, `internal/buildinfo/buildinfo_test.go` (new), `internal/server/server.go` (`/healthz`), `internal/server/server_test.go`, `internal/server/web_test.go`, `internal/server/contracts_test.go`, `internal/skill/skill_test.go`, `internal/cli/cli_test.go`. Verify: `go test -race ./internal/buildinfo/ ./internal/server/ ./internal/skill/ ./internal/cli/`; `make build-cli && ./bin/clavis version` on this checkout reports a `v0.1.0-rc.1.0.<time>-<sha>` pseudo-version with the full revision and the commit time; `make compile-server VERSION=v1.2.3 && ./bin/server` with a valid environment logs `server_started` with version `v1.2.3` and the checkout's commit and date; `go install ./cmd/clavis` into a temporary `GOBIN` from a `git archive HEAD | tar -x -C <dir>` export reports `dev` and `unknown`.

- [x] 1.1 Implement design decision 1 in `internal/buildinfo`: keep the three exported variables, add `resolve(version, commit, date string, read func() (*debug.BuildInfo, bool)) Info` with the per-value rules, compute `Current()` once through `sync.Once`, keep `Info` unchanged. Write `buildinfo_test.go` covering: all flags set with build information present (flags win); no flags with a tagged main module version and no VCS settings (proxy install); no flags with a pseudo-version and `vcs.revision`, `vcs.time` (checkout build); no flags with `(devel)` and no settings (test binary); no flags and the reader returning false (source export); version set with commit and date from settings (mixed); an empty main module version treated as absent.
- [x] 1.2 Change `/healthz` to report `buildinfo.Current().Version`, and the four test comparisons against `buildinfo.Version` to `buildinfo.Current().Version`; confirm `internal/cli/cli_test.go` still sees `"version":"dev"` under `go test`.
- [x] 1.3 Independent acceptance review of slice 1 against the resolution scenarios: run the four verifications above, confirm `/livez` and `/readyz` bodies are unchanged, confirm no production path other than `buildinfo` and the `/healthz` handler changed, record the revision and line counts. Reviewed at `ce0ab82`: ACCEPT WITH FOLLOW-UPS, no blocking findings. The reviewer ran the four verifications plus `gofmt`, the linter and the dead-code check, and confirmed the six scenarios against the code rather than the tests, the per-field precedence, unchanged `/livez` and `/readyz` bodies, no remaining raw `buildinfo.Version` read, and a race-free `sync.Once`. Three follow-ups applied before slice 2 closed: a case proving a stamped commit and date survive a checkout that would supply both, because deleting either guard had survived every test; a corrected comment, since a source export answers with `(devel)` rather than an unreadable reader; and an empty linker value such as `VERSION=` now folds onto the sentinel, so it falls back instead of reporting an empty version. Production Go +67/−3, tests +141/−4.

## 2. Stamped CLI, release workflow and documentation

Requirements: deployment-packaging "Build identity" (delta, "Stamped CLI"), "Tagged release" (all scenarios). Depends on slice 1. Owned paths: `Makefile` (`build-cli`, `IMAGE_FLAGS`), `scripts/build.test.mjs`, `.github/workflows/release.yml` (new), `README.md`, `deploy/charts/clavis/README.md`, `docs/PRD.md` section 12. Verify: `node --test scripts/build.test.mjs`; `make build-cli VERSION=v9.9.9 COMMIT=abc DATE=2026-09-16T00:00:00Z && ./bin/clavis version` reports all three; `GOOS=linux GOARCH=arm64 make build-cli && file bin/clavis` shows a Linux arm64 executable; `make build-cli NODE=false PNPM=false` still succeeds; `make image IMAGE_FLAGS="--platform linux/amd64,linux/arm64"` builds both without pushing; `.tools/helm/bin/helm package deploy/charts/clavis --version 0.1.0-rc.2 --app-version 0.1.0-rc.2` then `helm template` of the package shows image `ghcr.io/heurema/clavis:0.1.0-rc.2`; `make check`; `make smoke-image`.

- [x] 2.1 `build-cli`: add the `-ldflags` block from `compile-server` and honour `GOOS`/`GOARCH`; add `IMAGE_FLAGS ?=` to the `docker build` in `image`; extend `scripts/build.test.mjs` with one case asserting `build-cli` with the three variables reports them from `clavis version` and one asserting a `GOOS=linux GOARCH=arm64` build is a foreign executable.
- [x] 2.2 Write `.github/workflows/release.yml` per design decision 4, every `uses:` pinned by commit SHA with a version comment.
- [x] 2.3 Documentation per design decision 5: README Releases paragraph with both install paths, chart README OCI install command, PRD section 12 status paragraph.
- [x] 2.4 Independent acceptance review of slice 2 against "Stamped CLI" and "Tagged release": run every verification above, check each action SHA against its upstream tag, record the revision and line counts. Reviewed at `a989ee2`: ACCEPT WITH FOLLOW-UPS, no blocking findings. The reviewer traced all four release scenarios through the workflow, resolved each of the four action pins against its upstream tag, simulated every shell block under `set -eu` including a tag carrying a quote and a command substitution, and confirmed the two version forms, the stdin-only registry logins, the recursively expanded linker flags, the word-splitting image flags, the empty-value normalization and the documented checksum command on macOS. Four follow-ups applied before closing: `dist/` joins `.dockerignore` and `.gitignore`, because the release loop wrote about 120 MB of executables into the image build context; the commit is now peeled with `git rev-parse 'HEAD^{commit}'` rather than taken from `GITHUB_SHA`, which would silently stamp the tag object's own id for an annotated tag; one timestamp is resolved for the whole release instead of one per artifact; and a test comment claimed more than its assertion checks. Accepted as is: a failure after the image push can leave a published image and chart without a Release, which the design accepts and the first candidate rehearses; and a tag containing a quote would break the recipe, which falls under the agreed decision to add no tag guard. Production: workflow 130 lines, Makefile +11/−5, buildinfo +13; tests +87/−7; documentation +48/−1.

## 3. First release

- [x] 3.1 After merge, push `v0.1.0-rc.2` from `main` and watch the run. On success: make both GHCR packages public, then from a clean shell verify `go install github.com/heurema/clavis/cmd/clavis@v0.1.0-rc.2 && clavis version` reports the tag, download one binary with `SHA256SUMS`, verify the checksum and that it reports the same version, and `helm pull oci://ghcr.io/heurema/charts/clavis --version 0.1.0-rc.2`. Record the run URL and the image digest in the change before archiving. On failure, fix on `main` and tag `-rc.3`.

Released on 2026-09-16 as `v0.1.0-rc.5`, run https://github.com/heurema/clavis/actions/runs/35119870940, from `23385aa`. Image index `ghcr.io/heurema/clavis:0.1.0-rc.5` carrying `linux/amd64` and `linux/arm64`; chart `oci://ghcr.io/heurema/charts/clavis` version and appVersion `0.1.0-rc.5`, digest `sha256:33dacf3f3e734dad217cc0a5fbc14fea6823b0f66e7eb2c612e7b20aae78f41f`.

Verified after the run: `go install github.com/heurema/clavis/cmd/clavis@v0.1.0-rc.5` reports `v0.1.0-rc.5` with commit and date unknown, which is what a module proxy install can know; the downloaded `clavis_0.1.0-rc.5_darwin_arm64` matches `SHA256SUMS` and reports the same version with the full revision and the release timestamp; the image label carries `version=v0.1.0-rc.5` and `revision=23385aa…`, the peeled commit; the pulled chart renders `image: ghcr.io/heurema/clavis:0.1.0-rc.5`; and the Release is marked a pre-release with four executables and the checksums file.

Three earlier candidates failed and published nothing, each on a difference between a developer machine and a clean runner. `v0.1.0-rc.2` failed the quality check because an isolated mutation-input test compiled the whole module under a 120-second bound that only a warm build cache satisfies, fixed in #27. `v0.1.0-rc.3` failed because a plain `docker build` on the runner reaches the docker driver, which cannot span platforms, fixed in #28 by naming `docker buildx build`. `v0.1.0-rc.4` was refused by the registry: a container package named `clavis` already existed in the organization unlinked to any repository, so the workflow's token could not write to it; the owner granted the repository Actions access to that package, and no code changed. The failed tags were left in place rather than moved.