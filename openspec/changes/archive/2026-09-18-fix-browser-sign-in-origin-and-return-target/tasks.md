One slice, plus a real-browser verification that no automated test can replace. Estimate 40–70 handwritten production lines and 120–200 test lines; the template change regenerates templ output, reported separately.

## 1. Origin-preserving documents and a URL-carried return target

Requirements: local-authentication "Protected browser authentication" (delta: the referrer-policy rule, the refusal rule and both new scenarios, with the existing return-target scenarios kept green). Design decisions 1–3. Owned paths: `internal/web/auth_view.go` (failure text), `internal/server/auth.go` (`authorizePage`, the `/authorize` and `/login` timeout fallbacks, `loginBrowser`, `loginFailure`, the `fields` arithmetic), `internal/web/auth.templ` and `internal/web/auth_models.go` (the form action replaces the hidden field), generated templ output, `internal/server/auth_test.go`, `internal/server/authorize_test.go`, `internal/server/database_test.go`. Verify: `go test -race -p 1 -count=1 ./internal/server/ ./internal/web/`; `make check`; `make smoke`.

- [x] 1.1 Set `Referrer-Policy: strict-origin` on the authorize, invalid-link and sign-in documents, keeping `no-referrer` on the Approve 303, with a comment naming the opaque-origin failure it prevents (decision 1). Move the return target into the form action and have `loginFailure` derive it from the request URL (decision 2); drop `LoginModel.Next`, drop the `next` case from the body's field count, and delete the now-unneeded body-before-refusal reasoning. Tests: every refusal of `POST /login?next=<link>` (bad origin, `Authorization` header, wrong media type, duplicate session credentials, invalid input, wrong credentials, throttling, unavailable service, timeout fallback) renders a document whose form action carries the rebuilt link; a successful sign-in posted from such a document redirects to the link; an invalid, absent, repeated or oversized `next` renders a bare `/login` action and still refuses nothing else; `next` in the body is now an unknown field and is refused; the authorize, invalid-link and sign-in documents assert `strict-origin` while the Approve redirect asserts `no-referrer`; a POST carrying `Origin: null` is still refused with 403 everywhere; the refused-credentials, refused-origin, throttled and unavailable documents each carry exactly one failure sentence and no cleared-password line (decision 4).
- [x] 1.2 Independent acceptance review: run the verifications; confirm no path that renders the sign-in document can omit a target the request carried, including the timeout fallback; confirm the refusals still issue no session, set no cookie and call no service; probe `next` with an absolute URL, `//evil`, an encoded newline, `%2e%2e`, a second value and 8 KiB of padding, and confirm nothing but a rebuilt link reaches the action or any header; confirm `strict-origin` withholds path and query from `Referer` by reasoning over the header's definition and by checking no document sends its URL anywhere; kill hand mutants of the policy value and of the target derivation; record the revision and line counts with generated output separate. Implemented at `4166a09`: the policy is one commented constant on the authorize, invalid-link and approve-failure documents and both timeout fallbacks, `no-referrer` stays on the Approve 303 alone, `LoginModel.Next` became `Action`, and `web.loginAction` re-parses and rebuilds the action so the template can only render `/login` or `/login?next=<rebuilt link>`. Reviewed at the same revision: ACCEPT WITH FOLLOW-UPS, no blocking findings. The reviewer swept every route for the policy, probed about twenty hostile `next` values through the real handler and the real template (absolute, `//evil`, backslash, userinfo, encoded newline and CRLF, `%2e%2e` raw and decoded, doubled, fragment, quotes, `javascript:`, 8 KiB) and found nothing but a rebuilt link in any action or header, confirmed refusals set no cookie and call no service, that `Origin: null` is still refused on sign-in, logout and approve, and killed 20 of 20 mutants of the policy value and the target derivation. Applied after the review: the `/login` documents now state the policy themselves rather than relying on the browser default, since the sign-in URL carries the link, and the script test's formatting that failed `make check` was corrected. Accepted deviations: the approve failure documents also send `strict-origin`, because they carry a sign-out form; `FORBIDDEN` reads "This request is not permitted." everywhere, so the administration 403 loses its role clause while its heading and sign-out still give the context; `database_test.go`, `browser_login_database_test.go` and `scripts/browser-sign-in.mjs` changed because they submit the form; `loginLocation` folded into `loginAction`. Production +94/−40, tests +178/−49, generated templ +158/−172.
- [ ] 1.3 Run the flow by hand in **Safari, Firefox and Chrome** against a local server: `clavis login`, sign in, Approve, and confirm the CLI receives its session in each; in one browser refuse the first submission deliberately (post from a `no-referrer` document with `curl`, or block the cookie) and confirm the retry still returns to the approval page. Record each browser and its result here. This is the standing check for this flow, because no automated test sees a browser's `Origin`. Partially done on 2026-09-18 against a local server on the merged fix: the owner opened the link in a browser with an existing session, approved, and the CLI received its session (`GET /authorize` 200, `POST /authorize` 303, `POST /api/auth/token` 200, `Expires` thirty days out and `Idle until` seven). The same browser, before the fix, had been refused with 403 on the password form. Not yet run: the password-form path after the fix, a second and third browser, and the deliberate refusal. The archived record stands as the standing check to repeat.

## 2. Release

- [ ] 2.1 On the merged branch: `make check`, `go test -p 1 -count=1 ./...` against the test database, `make smoke`, and `openspec validate fix-browser-sign-in-origin-and-return-target --strict`. Record the results here, then tag `v0.1.0` with this fix included.
