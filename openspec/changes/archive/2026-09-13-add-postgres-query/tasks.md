Four slices in dependency order; implementation and independent acceptance review are separate tasks, and a reviewer never approves their own implementation. Depends on `remove-audit-journal` being archived first. Slice 1 lands first; slices 2 and 3 depend on it and can run in parallel (slice 3 against a fake executor); slice 4 needs everything. Expect roughly 800 to 1,100 changed handwritten lines; no generated output is expected.

## 1. Contracts and the PostgreSQL execute operation

Requirements: query-execution "Pass-through execution request" (session settings, connection lifecycle), "Structured results", "Bounds and explicit truncation", "Distinguishable failures" (mapping); connection-management "Provider registry" (execute operation, unsupported). Owned paths: `internal/auth/query.go` (DTOs, `QueryExecutor`, codes, bounds), `internal/auth/errors.go` (`Source`), `internal/provider/provider.go` (`Execute`, typed errors), `internal/provider/postgres.go` and `postgres_test.go` (execution with streaming drain, real-PostgreSQL tests), `internal/provider/victoriametrics.go` (unsupported), related `_test.go`. Verify: `go test -race ./internal/auth/ && CLAVIS_BACKEND_TEST_DATABASE_URL=... go test -race -p 1 ./internal/provider/`.

- [x] 1.1 Add the query DTOs, `QueryExecutor`, the five codes with their statuses and `error.source` rendering, the SQL bound and envelope allowance; verify JSON shapes (`null` values, list-always) and redaction.
- [x] 1.2 Add `Execute` to the provider interface with typed errors; implement PostgreSQL execution over the simple protocol with the session settings, type-name resolution, streaming drain with row and byte caps and the first-row rule, command tags, the statement index on source errors, timeout and connect-failure mapping, and the bounded close; VictoriaMetrics returns unsupported. Real-PostgreSQL tests: single and multi-statement scripts, implicit transaction rollback on a failing statement, typed values as text, duplicate column names, statements without rows, row cap and byte cap truncation with drain (writes after the cap still commit), `MaxRows` lowering, `statement_timeout` producing the timeout error, wrong password and unreachable host mapping, the secret never appearing in errors, unknown OID rendering, a context deadline shorter than the statement.
- [x] 1.3 Independent acceptance review of slice 1 including the drain and error-mapping paths; record the revision. Reviewed 2026-09-12 at `88ad813`: ACCEPT WITH FOLLOW-UPS, none in the provider's behaviour. Applied by the coordinator: the design's response-size assertion was wrong (the drain keeps the crossing row and any first row, and JSON escaping grows values), so the server serves the encoded response without a size check and the CLI reads a query response up to twice the byte-cap ceiling plus the allowance; the request deadline became a hung-connection backstop of ten times the statement timeout plus five seconds because PostgreSQL applies the timeout per statement of a script; the provider falls back to `clavis` for an empty application name and to the default caps for zero caps; the spec scenario for parse-time errors now says "grammatical error" and positions are offsets into the submitted string. Accepted decisions: the five codes live in `errors.go`; `error.source` is nested through custom JSON marshalling on the envelope with unknown fields still refused; a zero timeout defaults rather than disabling the bound. Handwritten +1,117/−25, no generated output.

## 2. Service

Requirements: query-execution "Pass-through execution request" (authorization first, unsupported before secrets, no pool); connection-grants "Per-request connection authorization" (inherited). Depends on slice 1. Owned paths: `internal/database/query.go`, `internal/database/query_test.go`, related tests. Verify: `CLAVIS_BACKEND_TEST_DATABASE_URL=... go test -race -p 1 ./internal/database/`.

- [x] 2.1 Implement `ExecuteQuery`: local validation with hints, `AuthorizeConnection`, provider lookup and `PROVIDER_UNSUPPORTED` before the secret, secret open and wipe, `Execute` with the record's bounds and the lowered `MaxRows`; map typed errors to codes; no platform write of any kind.
- [x] 2.2 Real-PostgreSQL tests: administrator and granted member succeed; ungranted member, disabled connection, revoked session and unsupported provider are refused with the documented codes and without opening the secret; truncated responses; source error, timeout, unreachable and refused-credentials codes; undecryptable secret; the platform database is unchanged by executions; no platform transaction open during the external call (assert via `pg_stat_activity` on the platform database).
- [x] 2.3 Independent acceptance review of slice 2; record the revision. Reviewed 2026-09-13 on the working tree over `3b82794`: ACCEPT WITH FOLLOW-UPS, all applied by the coordinator: the execution capability is now an optional `provider.Executor` interface decided by a type assertion (the worker had probed the provider with a cancelled context), with a registry test that VictoriaMetrics lacks it and PostgreSQL has it; the dial budget is capped at the operation timeout so a black-holed host fails within seconds instead of the hung-connection backstop; the probe shares the `wipe` helper; the member success test runs a script; the no-platform-transaction assertion counts any backend with an open transaction. Accepted: the second read of the row for the envelope after authorization (no lock, a deleted row answers `CONNECTION_NOT_FOUND`); `SOURCE_ERROR` carries no hint, every other failure a hint and no source. Handwritten about 610 lines added.

## 3. Route and CLI

Requirements: query-execution "Query route and command", "Distinguishable failures" (envelope); cli-authentication "Query command", "Bounded and non-disclosing authentication transport" (per-route bounds). Depends on slice 1; tests use a fake executor. Owned paths: `internal/server/query.go`, `internal/server/query_test.go`, `internal/server/auth.go` (route, rejection mapping, `HandlerWithAuth` dependency), `internal/server/server.go`, `cmd/server`, `internal/cli/query_commands.go`, `internal/cli/query_test.go`, `internal/cli/auth_transport.go` (per-route bounds, `routeFailures`, `source` decoding), `internal/cli/result.go` (rendering), `internal/cli/cli.go`, `internal/cli/*_test.go`. Verify: `go test -race ./internal/server/ ./internal/cli/ && make build-cli`.

- [x] 3.1 Mount `POST /api/query` with the shared handler shape, the SQL body bound, strict decoding, `error.source` in the envelope, and the defensive response-size assertion; require the executor in `HandlerWithAuth`.
- [x] 3.2 Add the `query` command with the three SQL inputs and local validation, per-route request and response bounds in the transport, the failure allowlist, strict validation of the results document, JSON and text rendering including source errors; unit and subprocess tests for every spec scenario, plus the real-database route round trip once slice 2 lands (coordinator).
- [x] 3.3 Independent acceptance review of slice 3; record the revision. Reviewed 2026-09-13 on the working tree over `a0a25b4`: ACCEPT WITH FOLLOW-UPS, all applied by the coordinator: the request body is read under the shared five-second bound even on the query route (the long budget is for waiting on the source); `--sql-stdin` honours cancellation while it waits; column names accept tabs and line breaks within PostgreSQL's identifier length while type names stay strict; a server-side `UNAUTHENTICATED` is covered on the query route. Coordinator additions during integration: the query route runs under `QueryRequestBudget` (ten times the statement-timeout ceiling plus ten seconds) with the write deadline extended, and the CLI's `query --timeout` defaults to it, because the five-second operation deadline could not hold a thirty-second statement; the response-size assertion was dropped and the CLI reads a query response up to twice the byte-cap ceiling plus the allowance; the executor seams were replaced by the store value; the real-database route round trip (`TestRealHTTPQueryRouteRoundTrip`) covers scripts, caps, both source-error shapes, refusals, revocation, the timeout and a statement outliving the operation deadline. Two raw control characters in test literals were escaped for the linter. Handwritten about +2,000/−50 (611 non-test).

## 4. Smoke, documentation and whole-change verification

Requirements: all delta specs end to end; project-bootstrap quality and smoke requirements. Depends on slices 1 to 3. Owned paths: `scripts/smoke.mjs`, `README.md`, `docs/PRD.md` (section 12 status), `reports/` (ignored). Verify: `make check && make smoke && make test-mutation`.

- [x] 4.1 Extend smoke with the query section from the design (script, truncation, `--max-rows`, syntax error, stdin and file inputs, denials, unsupported provider, timeout, platform rows unchanged).
- [x] 4.2 Update README (query section, bounds, failure codes, the role connection limit as the throttle, the catalog query for discovery) and PRD section 12; record accepted follow-ups (cursor optimisation, pooling).
- [x] 4.3 Run clean-checkout `make setup`, `make check`, the real-database race suite, `make smoke` and `make test-mutation` (with the documented extended budget); report handwritten and generated line counts separately; confirm no tracked source is rewritten by successful checks. Evidence 2026-09-13 at `7965d72` plus the whole-change follow-ups: `make check` passes without the database variable; `go test -race -p 1` for provider, database, server and CLI passes against PostgreSQL (one timing assertion in the query round trip was loosened from a six-second floor to "longer than the five-second operation deadline" after `pg_sleep(6)` measured 5.9 s in the full run); `make smoke` passes with the new query section (script, typed values, truncation at the cap and at `--max-rows`, stdin and file inputs, text output with `∅`, both source-error shapes, a server-refused cap exiting 2, unsupported provider, ungranted and disabled refusals, the source's own timeout, platform rows unchanged); a clean clone runs `make setup` and `make check` (the pre-existing lockfile drift only). `make test-mutation-full` with four workers completes in about 2,700 s: 783 killed, 106 lived, 501 not covered, 2 timed out, source unchanged (previous change 695 / 92 / 462 / 2; 88% of covered mutants killed). Lines since `main`: handwritten about +4,400/−80 across 37 files (tests about +2,500, smoke and docs about +260), no generated output. No tracked source was rewritten by any successful check.
- [x] 4.4 Whole-change independent review against the inherited requirements, all three delta specs and the design's inherited-decisions table, with the pass-through and bounds paths end to end; record the final revision, limits and any exceptions for owner acceptance before archive. Reviewed 2026-09-13 at `7965d72`: ACCEPT WITH FOLLOW-UPS, none blocking, all applied before archive: the CLI accepts source-failure text up to the envelope allowance instead of 4 KiB; a zero-column result set is accepted; a privilege refusal (`42501`) from the source is now tested at the provider layer; a non-numeric `--max-rows` is tested. Coverage table confirmed every delta scenario has a test or smoke assertion. Verified end to end: nothing parses, filters or wraps the SQL; authorization runs before the provider assertion, the second row read and the secret; bounds and drain semantics match the spec; the deadline layering (body under five seconds, request under the query budget, service backstop, capped dial) is consistent; no source text reaches a log; one connection per request, closed with its own deadline. Limits for the owner: a single value larger than about ten MiB cannot be retrieved through the CLI; a script with more than ten slow statements can reach the hung-connection backstop; a stale local `main` widens the diff-scoped mutation run. Follow-ups recorded: the cursor optimisation for single `SELECT`s and connection pooling.
