## 1. Repository and toolchain foundation

- [x] 1.1 Create the single Go module `github.com/heurema/clavis` with separate server and `clavis` CLI entry points and the React/TypeScript/Vite package under `web/`, using the Clavis naming, `CLAVIS_` environment prefix, and layout in the design. Verify each entry point builds independently and build outputs are ignored. Keep all implementation local; do not push without a later explicit user instruction.
- [x] 1.2 Recheck the design's September 9, 2026 release snapshot and pin the selected latest stable runtimes, package manager, backend dependencies (chi, urfave/cli, pgx, caarlos0/env, and Testify), bootstrap frontend dependencies (including TanStack Router and its Vite plugin, TanStack Query, Ky, and Zod), and PostgreSQL image/digest; create the required lockfiles and update the dated baseline in this change's OpenSpec design. Use Node 26 Current and TypeScript 7 with Oxlint plus its type-aware companion. Record sqlc/Goose for the first schema and React Hook Form/resolvers, TanStack Table, and Sonner for the first screens that need them; do not install unused selections or a standalone global state library. Verify clean installation succeeds without peer-dependency overrides and an intentionally mismatched frontend manifest/lockfile fails frozen installation without rewriting the lockfile.
- [x] 1.3 Integrate only the Tremor Raw controls needed by the setup page with the official Tailwind 4/Vite setup, using the source commit and dependency versions recorded in the design, recording upstream URLs and required notices. Adapt the copied source for the current React and tailwind-variants versions where needed. Verify a production build and browser render, including keyboard focus and light/dark styles, before building the full shell.
- [x] 1.4 Add isolated local PostgreSQL Compose configuration, `.env.example`, secret/output ignore rules, and database start/stop/reset commands. Verify loopback binding, a configurable host port, volume reuse after stop/start, and that reset targets only the project's development volume.

## 2. Server lifecycle and health

- [x] 2.1 Implement typed server environment parsing with caarlos0/env v11, semantic validation, and structured operational logging with allowed fields. Verify missing/empty database settings and malformed or non-positive durations fail before listening, and tests using secret-bearing configuration/errors/requests show no connection strings, credentials, bodies, or arbitrary query values in output.
- [x] 2.2 Add the pgx platform database pool and chi-routed liveness/readiness endpoints with standard HTTP handlers, the documented JSON responses, and bounded checks. Verify available, unavailable, timeout, and recovery behavior through httptest/Testify tests, including startup with a syntactically valid but unreachable database.
- [x] 2.3 Configure bounded HTTP handling and graceful termination, including cancellation and pool cleanup. Verify a process-level test stops accepting requests and exits within the configured bound while an in-flight dependency check is blocked.

## 3. CLI and machine-readable diagnostics

- [x] 3.1 Implement urfave/cli v3 command dispatch, offline help/version, build metadata defaults, the version 1 result envelope, and centralized output/exit handling. Verify offline help/version work and invalid commands/formats produce exactly one JSON document on stdout with exit code 2, no prompt, and no extra usage/error output.
- [x] 3.2 Implement `doctor` with URL configuration precedence, URL validation, request/body deadlines, response size limits, and status/body validation. Verify controlled HTTP responses cover ready, database unavailable, unreachable server, timeout, malformed/oversized responses, and invalid configuration with the specified states, codes, and exits, without echoing response bodies or credentials.
- [x] 3.3 Add the optional text rendering for version/doctor using the same result model. Verify equivalent success/failure states and exit codes in text mode while default JSON remains parseable and unaffected.

## 4. React and Tremor setup screen

- [x] 4.1 Build the one-page shell and setup/status view using TanStack Router file-based root/index routes, one TanStack Query client, and shared Tremor controls, with CLI guidance and no fabricated operational data. Configure the Vite router plugin and ensure route generation works before compiler checks from a clean workspace. Verify direct loading/reloading of `/` and the required loading, ready, dependency unavailable, and server unavailable states using isolated router/query clients in focused UI tests.
- [x] 4.2 Implement the typed readiness API function with the shared Ky client, Zod response validation, TanStack Query, and the local API proxy. Enforce a deadline covering response reading and forward cancellation. Disable Ky retries plus readiness automatic retries, polling, and focus/reconnect refetches; support fresh checks on page entry and manual Retry. Verify valid 503, transport failure, malformed/status-mismatched responses, timeout, absence of automatic repeat requests, and recovery through the real client, including a late response that must not overwrite a newer retry and safe errors that do not echo raw bodies.
- [x] 4.3 Implement system-default appearance and persisted light/dark selection with React state and a small context, accessible names, status announcements, and visible keyboard focus. Keep server data in Query and persist only the non-sensitive appearance preference; add no Zustand or other standalone store. Verify reload persistence and keyboard operation of appearance/retry controls in the browser; confirm labels explain status without relying only on color.

## 5. Developer workflow and integrated verification

- [x] 5.1 Wire the documented setup, development, and build commands, including safe local environment loading, loopback web binding, and configurable API proxy. Verify a contributor can start the database, server, and frontend using the documented commands without global installations, shell changes, company credentials, or external identity/model accounts.
- [x] 5.2 Add non-interactive formatting, static analysis, Go/Testify tests, frontend component tests, and production builds behind `make check`. Use a repository-pinned golangci-lint release with its standard linters, including govet, and expose `make lint-go`; install the checksum-verified release locally during setup or first use. Use TypeScript 7 compiler checks, Oxlint with type-aware TypeScript/React rules, Prettier, and the pinned Vitest/Testing Library/jsdom baseline. Use plain Go test functions and table-driven cases with require/assert and small fakes. Verify a clean run succeeds without rewriting source, and representative deliberate failures return nonzero and identify the failing stage.
- [x] 5.3 Implement the isolated `make smoke` runner with a unique Compose project/volume, available ports, compiled application processes, Playwright, bounded startup waits, and cleanup on success/failure. Verify real API/CLI/browser readiness, database stop/restart and recovery, and that the normal development volume and unrelated processes survive both a successful run and an injected smoke failure.
- [x] 5.4 Pin and install Gremlins locally, configure the selected handwritten mutation targets and generated/test-file exclusions, and add `make test-mutation` with fresh JSON reports, reporting-mode exit handling, resource bounds, and source cleanup. Verify compatibility with the selected Go toolchain using a real application-test run that detects an eligible mutation; verify baseline/tool/overall-timeout failures cannot appear successful, empty scope is explicit, and excluded files and original source remain unchanged. Review and record surviving mutations in ignored local notes without imposing a numerical score threshold.
- [x] 5.5 Keep the README short and simple: project purpose and current scope, required Go/Node/pnpm versions, quick start, essential commands, a CLI example, explicit browser-binary setup, and the consequence of resetting local database data. Keep technology decisions, frontend state responsibilities, the dependency baseline, and detailed tool behavior in the OpenSpec design; distinguish installed dependencies from selected tools deferred to later screens or schema work. Keep reviews and validation notes in ignored `.local/notes/` and generated reports in ignored `reports/`, outside maintained `docs/`. Verify the full documented setup, build, check, smoke, and mutation sequence from a clean workspace with required tools already installed, and record the results locally.
