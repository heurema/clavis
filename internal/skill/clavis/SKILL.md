---
name: clavis
description: Query the company's PostgreSQL, VictoriaMetrics and VictoriaLogs sources through the clavis CLI, which forwards each query as typed, applies the connection's stored credentials and bounds the answer.
---

# Clavis

Placeholder entry; the skill content slice replaces it with the real one.
Start by listing the connections you may use and read the reference named by
each connection's `provider` field.

```
clavis connections list --output text
```
