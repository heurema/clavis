---
name: clavis-postgresql
description: Placeholder reference for querying a PostgreSQL connection through clavis.
---

# PostgreSQL

Placeholder reference; the skill content slice replaces it with the real one.
Discover the catalog first, then put ordering and limits inside the SQL.

```
clavis query --connection <ref> --sql "select 1" --output text
```
